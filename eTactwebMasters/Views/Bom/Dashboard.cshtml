@*@model System.Data.DataTable*@
@model eTactWeb.DOM.Models.BomDashboard

@{
    ViewData["Title"] = "Dashboard";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}


<!-- Modal -->
<div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header bg-gradient" style="background-color: teal;">
                <h5 class="modal-title text-light" id="staticBackdropLabel">Bom<strong> - Detail</strong></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body bg-gradient" style="background-color: mintcream;">
                <div class="row rounded overflow-auto pb-0">
                    <table class="table table-hover table-bordered table-striped">
                        <thead class="bg-gradient card-headerBG text-light small" style="white-space:nowrap">
                            <tr>
                                <th>Sr#</th>
                                <th>SeqNo</th>
                                <th>BomNo</th>
                                <th>BomQty</th>
                                <th>FinishItemCode</th>
                                <th>Item_Name</th>
                                <th>RM Code</th>
                                <th>RM Name</th>
                                <th>BOMName</th>
                                <th>Qty</th>
                                <th>Unit</th>
                                <th>UsedStageId</th>
                                <th>AltName1</th>
                                <th>AltQty1</th>
                                <th>AltName2</th>
                                <th>AltQty2</th>
                                <th>IssueToJoBwork</th>
                                <th>RecFrmCustJobWork</th>
                                <th>PkgItem</th>
                                <th>Remark</th>
                                <th>GrossWt</th>
                                <th>NetWt</th>
                                <th>Scrap</th>
                                <th>RunnerItemCode</th>
                                <th>RunnerQty</th>
                                <th>BurnQty</th>
                                <th>EntryDate</th>
                                <th>EffectiveDate</th>
                            </tr>
                        </thead>
                        <tbody id="divModel">
                            <partial name="_BomDetail" />
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="modal-footer bg-gradient" style="background-color: mintcream;">
                <button type="button" class="btn btn-danger" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<div class="shadow-lg">
    <div class="card rounded border-light bg-gradient">
        <div class="card-header card-headerBG text-light bg-gradient">
            <h5>
                Bom - <strong>Dashboard</strong>
                <span class="d-flex float-end" style="margin-top:-2px;">
                    <a asp-action="BomForm" asp-route-ID="0" class="btn btn-sm btn-outline-light bg-gradient" id="savebtn">
                        <i class="fa fa-plus-circle fafw fa-lg" aria-hidden="true"></i>
                        Add New
                    </a>
                </span>
            </h5>
        </div>

        <div class="card-body">
            <div class="card overflow-auto">
                <div class="card-body">
                    <form id="SParam">
                        <div class="row">
                            <div class="col-lg-2 col-md-4 col-sm-12 col-xs-12">
                                <label asp-for="FGPartCode" class="form-label">FG Part Code</label>
                                <input type="text" class="form-control" asp-for="FGPartCode" />
                            </div>
                            <div class="col-lg-2 col-md-4 col-sm-12 col-xs-12">
                                <label asp-for="FGItemName" class="form-label">FG Item Name</label>
                                <input type="text" class="form-control" asp-for="FGItemName" />
                            </div>
                            <div class="col-lg-2 col-md-4 col-sm-12 col-xs-12">
                                <label class="form-label">RM Part Code</label>
                                <input type="text" class="form-control" asp-for="RMPartCode" />
                            </div>
                            <div class="col-lg-2 col-md-4 col-sm-12 col-xs-12">
                                <label asp-for="RMItemName" class="form-label">RM Item Name</label>
                                <input type="text" class="form-control" asp-for="RMItemName" />

                            </div>
                            <div class="col-lg-2 col-md-4 col-sm-12 col-xs-12" style="width:60px;">
                                <label asp-for="BomRevNo" class="form-label">RevNo</label>
                                <input type="text" class="form-control" asp-for="BomRevNo" style="width:50px;" />
                            </div>
                            <div class="col-lg-2 col-md-4 col-sm-12 col-xs-12" style="width:150px;">
                                <label asp-for="DashboardType"  class="form-label">Type</label>
                                <select asp-for="DashboardType" id="DashboardType"  class="form-select" onchange="GetSearchData();">
                                    <option value="0" >Select</option>
                                    <option value="Summary" selected>Summary</option>
                                    <option value="Detail">Detail</option>
                                </select>
                            </div>
                            <div class="col-sm-2 col-md-6 col-lg-4 col-xl-2">
                                <label class="form-label">Global search</label>
                                <input type="text" class="form-control" id="search-box" style="background-color:#FFFACD" placeholder="Search...">
                            </div>

                            <div class="col-lg-3 col-md-8 col-sm-6 col-xs-12 d-flex justify-content-evenly mt-3" style="margin-left:20px;">
                                <button class="btn btn-sm btn-outline-info" type="button" onclick="GetSearchData();">
                                    <i class="fa-solid fa-magnifying-glass fa-fw fa-lg "></i>SEARCH
                                </button>
                                <button class="btn btn-sm btn-outline-success" style="padding:3px;height:30px;width:100px;margin-left:20px;" type="button" id="sendToExcel">
                                    <i class="fa-solid fa-upload fa-fw fa-lg "></i>EXCEL
                                </button>
                                <a class="btn btn-sm btn-outline-danger" asp-controller="Bom" asp-action="Dashboard">
                                    <i class="fa-solid fa-rotate fa-fw fa-lg"></i>RESET
                                </a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="card-body pb-0 pt-0">
            <div class="progress" style="height: 4px;">
                <div class="progress-bar progress-bar-striped bg-info" role="progressbar" style="width: 100%" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
            </div>
        </div>
        <div class="card-body">
            <div class="card overflow-auto">
                <div class="card-body" id="divDashboardGrid">
                    <partial name="_BomDashboardGrid" />
                </div>
               
            </div>
        </div>
       @*  pagination *@
        <div class="card-footer d-flex justify-content-between align-items-center">
            @* <div>
                <button type="button" class="btn btn-link bg-graident" onclick="window.location.back()">
                    <i class="fa fa-chevron-circle-left fa-fw fa-lg" aria-hidden="true"></i>BACK
                </button>
            </div> *@
            @{
                int totalPages = (int)Math.Ceiling((double)Model.TotalRecords / Model.PageSize);
            }
            @if (Model.TotalRecords > 0) // Ensure pagination always shows when records exist
            {
                <ul class="pagination d-flex align-items-center">
                    @if (Model.PageNumber > 1)
                    {
                        <li class="page-item">
                            <a class="page-link" href="@Url.Action("Dashboard", new { pageNumber = Model.PageNumber - 1, pageSize = Model.PageSize })">Prev</a>
                        </li>
                    }

                    @if (Model.PageNumber > 3)
                    {
                        <li class="page-item"><a class="page-link" href="@Url.Action("Dashboard", new { pageNumber = 1, pageSize = Model.PageSize })">1</a></li>
                        <li class="page-item disabled"><a class="page-link">...</a></li>
                    }

                    @for (int i = Math.Max(1, Model.PageNumber - 2); i <= Math.Min(Model.PageNumber + 2, totalPages); i++)
                    {
                        <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                            <a class="page-link" href="@Url.Action("Dashboard", new { pageNumber = i, pageSize = Model.PageSize })">@i</a>
                        </li>
                    }

                    @if (Model.PageNumber < totalPages - 2)
                    {
                        <li class="page-item disabled"><a class="page-link">...</a></li>
                        <li class="page-item">
                            <a class="page-link" href="@Url.Action("Dashboard", new { pageNumber = totalPages, pageSize = Model.PageSize })">@totalPages</a>
                        </li>
                    }

                    @if (Model.PageNumber < totalPages)
                    {
                        <li class="page-item">
                            <a class="page-link" href="@Url.Action("Dashboard", new { pageNumber = Model.PageNumber + 1, pageSize = Model.PageSize })">Next</a>
                        </li>
                    }
                </ul>
            }
        </div>
    </div>
</div>

@section Scripts
{
    <script>


        $(document).ready(function () {
            $('#DashboardType').select2();
            var fgPartCodeBack = localStorage.getItem('FGPartCodeBack');
            var fgItemNameBack = localStorage.getItem('FGItemNameBack');
            var rmPartCodeBack = localStorage.getItem('RMPartCodeBack');
            var rmItemNameBack = localStorage.getItem('RMItemNameBack');
            var bomRevNoBack = localStorage.getItem('BomRevNoBack');
            var summaryDetailBack = localStorage.getItem('SummaryDetailBack');
            var globalSearchBack = localStorage.getItem('GlobalSearchBack');

            GetFormRights();

            $('#FGPartCode').val(fgPartCodeBack == "" ? $('#FGPartCode').val() : fgPartCodeBack);
            $('#FGItemName').val(fgItemNameBack == "" ? $('#FGItemName').val() : fgItemNameBack);
            $('#RMPartCode').val(rmPartCodeBack == "" ? $('#RMPartCode').val() : rmPartCodeBack);
            $('#RMItemName').val(rmItemNameBack == "" ? $('#RMItemName').val() : rmItemNameBack);
            $('#BomRevNo').val(bomRevNoBack == "" ? $('#BomRevNo').val() : bomRevNoBack);
            $('#DashboardType').val(summaryDetailBack == "" ? $('#DashboardType').val() : summaryDetailBack);
            $('#search-box').val(globalSearchBack == "" ? $('#search-box').val() : globalSearchBack);
            var dashGrid = $('#divDashboardGrid tr').length;
            $('#totalRows').text(dashGrid);

            localStorage.removeItem('FGPartCodeBack');
            localStorage.removeItem('FGItemNameBack');
            localStorage.removeItem('RMPartCodeBack');
            localStorage.removeItem('BomRevNoBack');
            localStorage.removeItem('SummaryDetailBack');
            localStorage.removeItem('GlobalSearchBack');
        });
        AutoComplete('VBOM', 'FGPartCode', 'FGPartCode', null);
        AutoComplete('VBOM', 'FGItemName', 'FGItem', null);
        AutoComplete('VBOM', 'RMPartCode', 'RMPartCode', null);
        AutoComplete('VBOM', 'RMItemName', 'RMItemName', null);
        AutoComplete('VBOM', 'BomRevNo', 'BomNo', null);

        $('#FGPartCode').keyup(function () {
            AutoComplete('VBOM', 'FGPartCode', 'FGPartCode', null);
        });
        $('#FGItemName').keyup(function () {
            AutoComplete('VBOM', 'FGItemName', 'FGItem', null);

        });
        $('#RMPartCode').keyup(function () {
            AutoComplete('VBOM', 'RMPartCode', 'RMPartCode', null);
        });
        $('#RMItemName').keyup(function () {
            AutoComplete('VBOM', 'RMItemName', 'RMItemName', null);
        });
        async function AutoComplete(Schema, CtrlID, ColName, SelectFunc) {

            $("#" + CtrlID).autocomplete({
                source: await function (request, response) {
                    $.ajax({
                        // url: '/Home/AutoComplete',
                          url: '@Url.Action("AutoComplete", "Bom")',
                        type: "POST",
                        data: { "Schema": Schema, "ColName": ColName, "prefix": request.term },
                        success: function (data) {
                            response($.map(data, function (key, value) {
                                return key.text;
                            }));
                        },
                        error: function (response) {
                            $.notify(response.responseText, { position: "top center", className: "error" });
                        },
                        failure: function (response) {
                            $.notify(response.responseText, { position: "top center", className: "error" });
                        }
                    });
                },
                select: function (e, i) {
                    $("#" + SelectFunc).val(i.item.value);
                },
                minLength: 1
            });
        }
        $("#search-box").on("input", function () {
             debugger
            var searchString = $(this).val().trim();
            var ReportType = $("#DashboardType").val();

            $.ajax({
                url: "/Bom/GlobalSearch?searchString=" + searchString + "&ReportType=" + ReportType,
                type: "GET",
                data: { searchString: searchString },
                success: function (data) {
                    console.log(data);
                    $('#divDashboardGrid').empty();
                    $('#divDashboardGrid').html(data);
                    //$('.ShowAllDiv').empty();
                    //$('.ShowAllDiv').append(data);
                    var dashGrid = $('#divDashboardGrid tr').length;
                    $('#totalRows').text(dashGrid);
                },
                error: function () {
                    console.log("Error fetching search results.");
                }
            });
        });

        function GetFormRights() {
            $.ajax({
                url: "@Url.Action("GetFormRights", "Bom")",
                type: "POST",
                success: function (data) {
                    if (data != null) {

                        var obj = $.parseJSON(data);
                        if (obj.Result.Table.length == 0) {
                            var rowCounter = $('#divDashboardGrid tr').length;
                            for (var i = 0; i < rowCounter; i++) {
                                $('#viewCursorDiv-' + i).empty();
                                $('#viewCursorDiv-' + i).append(`<a class="table-link link-success noCursor" id="viewNoCursor"">
                                                                                                <span class="fa-stack">
                                                                                                    <i class="fa fa-square fa-stack-2x"></i>
                                                                                                    <i class="fa-solid fa-magnifying-glass fa-stack-1x fa-inverse"></i>
                                                                                                </span>
                                                                                            </a>`);
                            }
                            for (var i = 0; i < rowCounter; i++) {
                                $('#editCursorDiv-' + i).empty();
                                $('#editCursorDiv-' + i).append(`<a class="table-link link-primary noCursor">
                                                                                                        <span class="fa-stack">
                                                                                                            <i class="fa fa-square fa-stack-2x"></i>
                                                                                                            <i class="fa fa-pencil fa-stack-1x fa-inverse"></i>
                                                                                                        </span>
                                                                                                    </a>`);
                            }
                            for (var i = 0; i < rowCounter; i++) {
                                $('#deleteCursorDiv-' + i).empty();
                                $('#deleteCursorDiv-' + i).append(`<a class="table-link link-danger noCursor">
                                                                                                <span class="fa-stack">
                                                                                                    <i class="fa fa-square fa-stack-2x"></i>
                                                                                                    <i class="fa-solid fa-trash fa-stack-1x fa-inverse"></i>
                                                                                                </span>
                                                                                            </a>`);
                            }

                        } else {
                            console.log(obj);
                            if (obj.Result.Table[0] != undefined) {
                                var optAll = obj.Result.Table[0].OptAll;
                                var optSave = obj.Result.Table[0].OptSave;
                                var optUpdate = obj.Result.Table[0].OptUpdate;
                                var optDelete = obj.Result.Table[0].OptDelete;
                                var optView = obj.Result.Table[0].OptView;
                                var rowCounter = $('#divDashboardGrid tr').length;
                                if (!optAll) {
                                    if (!optSave) {
                                        $("#savebtn").css("pointer-events", "none");
                                    } else {
                                        $("#savebtn").css("pointer-events", "");
                                    }
                                    if (!optView) {

                                        for (var i = 0; i < rowCounter; i++) {
                                            $('#viewCursorDiv-' + i).empty();
                                            $('#viewCursorDiv-' + i).append(`<a class="table-link link-success noCursor" id="viewNoCursor"">
                                                                                                <span class="fa-stack">
                                                                                                    <i class="fa fa-square fa-stack-2x"></i>
                                                                                                    <i class="fa-solid fa-magnifying-glass fa-stack-1x fa-inverse"></i>
                                                                                                </span>
                                                                                            </a>`);
                                        }
                                    }
                                    if (!optUpdate) {
                                        for (var i = 0; i < rowCounter; i++) {
                                            $('#editCursorDiv-' + i).empty();
                                            $('#editCursorDiv-' + i).append(`<a class="table-link link-primary noCursor">
                                                                                                <span class="fa-stack">
                                                                                                    <i class="fa fa-square fa-stack-2x"></i>
                                                                                                    <i class="fa fa-pencil fa-stack-1x fa-inverse"></i>
                                                                                                </span>
                                                                                            </a>`);
                                        }
                                    }
                                    if (!optDelete) {
                                        for (var i = 0; i < rowCounter; i++) {
                                            $('#deleteCursorDiv-' + i).empty();
                                            $('#deleteCursorDiv-' + i).append(`<a class="table-link link-danger noCursor">
                                                                                                <span class="fa-stack">
                                                                                                    <i class="fa fa-square fa-stack-2x"></i>
                                                                                                    <i class="fa-solid fa-trash fa-stack-1x fa-inverse"></i>
                                                                                                </span>
                                                                                            </a>`);
                                        }
                                    }
                                }
                            }
                        }
                    }
                },
                error: function (errMsg) {
                    $.notify(errMsg, { position: "top center", className: "error" });
                }
            });
        }

        function GetBomDetail(FGC, BMNo) {

            $.ajax({
                url: '@Url.Action("GetBomDetail", "Bom")',
                type: "POST",
                data: { FGC: FGC, BMNo: BMNo },
                success: function (data) {

                    $('#divModel').html(data);
                },
                error: function (response) {
                    alert(response.responseText);
                },
                failure: function (response) {
                    alert(response.responseText);
                }
            });
        }

        // function confirmDelete(event) {

        //     event.preventDefault();
        //     if (confirm("Are you sure you want to delete this entry?")) {
        //         var linkUrl = $(event.currentTarget).attr("href");
        //         window.location.href = linkUrl;
        //     }
        // }

        function back(event) {
            event.preventDefault();
             
            var linkurl = $(event.currentTarget).attr("href");
            linkurl = linkurl + "&FGPartCode=" + $('#FGPartCode').val() + "&FGItemName=" + $('#FGItemName').val() + "&RMPartCode=" + $('#RMPartCode').val() + "&RMItemName=" + $('#RMItemName').val() + "&BomRevNo=" + $('#BomRevNo').val() + "&DashboardType=" + $('#DashboardType').val() + "&GlobalSearch=" + $('#search-box').val();
            window.location.href = linkurl;
        }

        function confirmdelete(event) {
            event.preventDefault();
            if (confirm("are you sure you want to delete this entry?")) {
                var linkurl = $(event.currentTarget).attr("href");
                linkurl = linkurl + "&FGPartCode=" + $('#FGPartCode').val() + "&FGItemName=" + $('#FGItemName').val() + "&RMPartCode=" + $('#RMPartCode').val() + "&RMItemName=" + $('#RMItemName').val() + "&BomRevNo=" + $('#BomRevNo').val() + "&DashboardType=" + $('#DashboardType').val() + "&GlobalSearch=" + $('#search-box').val();
                window.location.href = linkurl;
            }
        }

        function GetSearchData() {
            if ($('#DashboardType').val() == "Summary") {
                GetSummaryData();
            } else {
                GetDetailData();
            }
        }

        function GetSummaryData() {
            var data = $('#SParam').serialize();
            $.ajax({
                url: '@Url.Action("GetSearchData", "Bom")',
                type: "POST",
                data: $('#SParam').serializeArray(),
                success: function (data) {
                    $('#divDashboardGrid').empty().append(data);
                    GetFormRights();
                },
                error: function (response) {
                    alert(response.responseText);
                },
                failure: function (response) {
                    alert(response.responseText);
                }
            });
        }

        function GetDetailData() {
            var data = $('#SParam').serialize();
            $.ajax({
                url: '@Url.Action("GetDetailSearchData", "Bom")',
                type: "POST",
                data: $('#SParam').serializeArray(),
                success: function (data) {
                    $('#divDashboardGrid').empty().append(data);
                    GetFormRights();
                },
                error: function (response) {
                    alert(response.responseText);
                },
                failure: function (response) {
                    alert(response.responseText);
                }
            });
        }
        $('#sendToExcel').click(function () {
            $("#bomTable").table2excel({
                filename: "BomDetail.xls"
            });
        });

        $(document).on("click", ".sortCol", function () {
            ;
            var $column = $(this).parent().closest("th");
            var colno = $(this).data('columnno');
            var currentOrder = $(this).data('sortorder');

            $('.sortCol').css('color', 'white');
            if (currentOrder === 'asc') {
                sortTable(colno, false);
                $(this).data('sortorder', 'desc');
                $(this).css('color', 'yellow'); // Visual indication of descending order
                //$(this).css('font-size', 'larger'); // Visual indication of descending order
                $(this).find('i:first').removeClass('fa-arrow-up');
                $(this).find('i:first').addClass('fa-arrow-down');
            } else {
                sortTable(colno, true);
                $(this).data('sortorder', 'asc');
                $(this).css('color', 'yellow');
                // $(this).css('font-size', 'larger'); // Visual indication of ascending order
                $(this).find('i:first').removeClass('fa-arrow-down');
                $(this).find('i:first').addClass('fa-arrow-up');
            }
        });
        function sortTable(columnIndex, ascending) {

            var table = $('#bomTable');
            var rows = table.find('#bomTableBody > tr').toArray();

            rows.sort(function (a, b) {
                var aValue = $(a).find('td').eq(columnIndex).text();
                var bValue = $(b).find('td').eq(columnIndex).text();

                var aIntValue = 0;
                var bIntValue = 0;
                if (/^\d*\.?\d+$/.test(aValue) && /^\d*\.?\d+$/.test(bValue)) {
                    aIntValue = parseInt(aValue, 10);
                    bIntValue = parseInt(bValue, 10);
                }

                if (aIntValue > 0) {
                    if (ascending) {
                        return aValue - bValue;
                    } else {
                        return bValue - aValue;
                    }
                } else {
                    if (ascending) {
                        return aValue.localeCompare(bValue);
                    } else {
                        return bValue.localeCompare(aValue);
                    }
                }
            });

            $.each(rows, function (index, row) {
                table.children('#bomTableBody').append(row);
            });
        }


        var currentSearchColumn = null;
        $("#search-box").on("keyup", function () {
            var value = $(this).val().toLowerCase();
            $("#bomTableBody tr").filter(function () {
                if (currentSearchColumn !== null) {
                    var columnText = $(this).find('td').eq(currentSearchColumn).text().toLowerCase();
                    $(this).toggle(columnText.indexOf(value) > -1);
                } else {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
                }
            });
        });


        $(document).on("click", "th[data-columnno]", function () {
            currentSearchColumn = $(this).data('columnno');
            $("#search-box").val('').trigger('keyup'); // Reset search box and trigger the search
        });

    </script>
    <script src="https://cdn.jsdelivr.net/npm/jquery-table2excel@1.1.1/dist/jquery.table2excel.min.js"></script>

}

<style>
    .pagination {
        display: flex;
        justify-content: center;
        align-items: center;
        list-style: none;
        padding: 0;
        margin: 20px auto;
        gap: 5px; /* Space between buttons */
        white-space: nowrap; /* Keeps it in a single line */
    }

    .page-item {
        display: inline-block;
    }

    .page-link {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 40px;
        height: 40px;
        text-decoration: none;
        color: black;
        border: 1px solid #ddd;
        font-size: 16px;
        background-color: white;
        border-radius: 5px;
        transition: background 0.3s;
    }

    .page-item.active .page-link {
        background-color: #4CAF50;
        color: white;
        border: 1px solid #4CAF50;
    }

    .page-item.disabled .page-link {
        background-color: #f0f0f0;
        color: #999;
        cursor: default;
    }

    .page-link:hover:not(.disabled):not(.active) {
        background-color: #ddd;
    }

</style>