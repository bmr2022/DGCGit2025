@model eTactWeb.DOM.Models.PSApprovalModel

@{
    ViewData["Title"] = "PS Approval";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}
<div class="shadow-lg">
    <div class="card rounded border-light bg-gradient">
        <div class="card-header card-headerBG text-light bg-gradient">
            <h5>
                PS Approval
                <span class="d-flex float-end" style="margin-top:-2px;">
                    @*<a asp-action="ReqWithoutBom" asp-route-ID="0" class="btn btn-sm btn-outline-light bg-gradient">
                    <i class="fa fa-plus-circle fafw fa-lg" aria-hidden="true"></i>
                    Add New
                    </a>*@
                </span>
            </h5>
        </div>
        <div class="card-body">
            <div class="card overflow-auto">
                <div class="card-body">
                    <form id="SParam">
                        <div class="row">
                            <div class="col-sm-3 col-md-6 col-lg-2 col-xl-4">
                                <div class="row">
                                    <div class="col">
                                        <label class="form-label">From Date</label>
                                        <input type="hidden" value="@Model.FromDate" id="finFromDate" />
                                        <input type="hidden" value="@Model.ToDate" id="finToDate" />
                                        <input type="hidden" value="@Model.ApprovalType" id="selectedApprovalType" />
                                        <input asp-for="FromDate" maxlength="10" class="form-control" value="@Model.FromDate" />
                                    </div>
                                    <div class="col">
                                        <label class="form-label">To Date</label>
                                        <input asp-for="ToDate" maxlength="10" class="form-control" value="@Model.ToDate" />
                                    </div>
                                </div>
                            </div>
                            <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
                                <label asp-for="ApprovalType" class="form-label">Approval Type</label>
                                <select class="form-select" asp-for="ApprovalType">
                                    <option value="" disabled>-Select-</option>
                                    <option value="listofUnApp" selected>List of unapproved PS</option>
                                    <option value="listofUnAppAmd">List of unapproved amendment</option>
                                    <option value="listofApp" >List of Approved PS</option>
                                    @*<option value="listofAppAmd">List of approved amendment</option>*@
                                </select>
                                <input type="hidden" id="approvalType" value="First Level Approval" />
                            </div>
                            <div class="col-xl-2 col-lg-3 col-md-3 col-sm-6 col-xs-12">
                                <label asp-for="PONo" class="form-label">PO NO</label>
                                <input asp-for="PONo" class="form-control" />
                            </div>
                            <div class="col-xl-1 col-lg-3 col-md-3 col-sm-6 col-xs-12">
                                <label asp-for="SchNo" class="form-label">Sch NO</label>
                                <input asp-for="SchNo" class="form-control" />
                            </div>
                            <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
                                <label asp-for="VendorName" class="form-label">Vendor Name</label>
                                <input asp-for="VendorName" class="form-control" />
                            </div>
                            <div class="col-xl-1 col-lg-3 col-md-3 col-sm-6 col-xs-12">
                                <label asp-for="CC" class="form-label">Branch</label>
                                <input asp-for="CC" class="form-control" value="@Model.CC" readonly />
                            </div>

                        </div>
                        <div class=" d-flex justify-content-center mt-3" width="inherit">
                            <button class="btn btn-sm btn-outline-info" style="padding:3px;height:30px;width:100px;margin-left:20px;" type="button" onclick="GetListOfApprovedPS();">
                                <i class="fa-solid fa-magnifying-glass fa-fw fa-lg "></i>SHOW
                            </button>
                            <button class="btn btn-sm btn-outline-success" style="padding:3px;height:30px;width:100px;margin-left:20px;" type="button" id="sendToExcel">
                                <i class="fa-solid fa-upload fa-fw fa-lg "></i>EXCEL
                            </button>
                            <a class="btn btn-sm btn-outline-danger" style="padding:3px;height:30px;width:100px;margin-left:20px;" asp-controller="PSApproval" asp-action="PSApproval">
                                <i class="fa-solid fa-rotate fa-fw fa-lg"></i>RESET
                            </a>

                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="progress my-3 mb-3" style="height: 4px;">
            <div class="progress-bar progress-bar-striped bg-warning" role="progressbar" style="width: 100%" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
        </div>
        <div class="card-body">
            <div class="card overflow-auto">
                <div class="card-body" id="divDashboardGrid">
                    <partial name="_PSApprovalGrid" />
                </div>
            </div>
        </div>
        <center><strong>TOTAL ROWS : <span id="totalRows"></span></strong></center>
    </div>
</div>

@section Scripts
    {
        <script>
            $(document).ready(function(){
                 
                $(document).on("click", ".redirectToApproval", function (e) {
                     
                    e.preventDefault();
                    var newUrl = this.href;
                    newUrl = newUrl + "&TypeOfApproval=" + $('#ApprovalType').val();
                    window.location.href = newUrl;
                });

            if ($('#selectedApprovalType').val()=='Approval'){
                $('#ApprovalType').val('listofUnApp').change();
            } if ($('#selectedApprovalType').val() == 'UnApproval') {
                $('#ApprovalType').val('listofApp').change();

            } if ($('#selectedApprovalType').val() == 'Unapproving Amendment') {
                $('#ApprovalType').val('listofAppAmd').change();

            } if ($('#selectedApprovalType').val() == 'Approving Amendment') {
                $('#ApprovalType').val('listofUnAppAmd').change();

            }
            });

            $(function () {

                var currentDate = new Date();
                var firstDayOfMonth = new Date(currentDate.getFullYear(), currentDate.getMonth(), 1);
                function formatDate(date) {
                    var day = ("0" + date.getDate()).slice(-2);
                    var month = ("0" + (date.getMonth() + 1)).slice(-2);
                    var year = date.getFullYear();
                    var dateString = day + "/" + month + "/" + year;
                    return dateString;
                }
                //$('#FromDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", 'now');

                $('#FromDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", formatDate(firstDayOfMonth));
                $('#ToDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", 'now');

                AutoComplete('VPURCHASESCHEDULE', 'VendorName', 'VendorName', null);
                AutoComplete('VPURCHASESCHEDULE', 'PONo', 'PONO', null);
                AutoComplete('VPURCHASESCHEDULE', 'SchNo', 'SchNo', null);
                AutoComplete('VPURCHASESCHEDULE', 'CC', 'CC', null);
                GetTotalRows();
                GetListOfApprovedPS();
                //GetAllowedAction();
            });

            //function GetAllowedAction() {
            //    $.ajax({
            //        type: "GET",
            //        url: "@Url.Action("GetAllowedAction")",
            //        dataType: "json",
            //        async: false,
            //        success: function (result, status, error) {
            //            if (result != null) {
            //                var obj = $.parseJSON(result);
            //                if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
            //                    console.log(obj);
            //                    var action = [];
            //                    for (var i = 0; i < obj.Result.Table.length; i++) {
            //                        action.push(obj.Result.Table[i].Action);
            //                    }
            //                    console.log(action);
            //                    if (action.includes('FinalLevelApproval')) {
            //                        //do nothing
            //                    } else {
            //                        $("#ApprovalType option[value='finalApproval']").prop("disabled", true);
            //                    }
            //                    if (action.includes('AmendmentApproval')) {
            //                        //do nothing
            //                    } else {
            //                        $("#ApprovalType option[value='amdApproval']").prop("disabled", true);
            //                    }
            //                    if (action.includes('FirstLevelApproval')) {
            //                        //do nothing
            //                    } else {
            //                        $("#ApprovalType option[value='firstLvlApproval']").prop("disabled", true);

            //                    } if (action.includes('FirstLevelUnapproval')) {
            //                        //do nothing
            //                    } else {
            //                        $("#ApprovalType option[value='firstLvlUnApp']").prop("disabled", true);

            //                    } if (action.includes('FinalLevelUnapproval')) {
            //                        //do nothing
            //                    } else {
            //                        $("#ApprovalType option[value='finalUnApp']").prop("disabled", true);

            //                    }
            //                }

            //            }
            //        }
            //    });
            //}
        

            function GetListOfApprovedPS() {
                $.ajax({
                    type: "GET",
                    url: "@Url.Action("GetListOfApprovedPS")",
                    type: "POST",
                    data:{FromDate:$('#FromDate').val(),ToDate:$('#ToDate').val(),ApprovalType: $('#ApprovalType').val(),PONO : $('#PONo').val(),SchNo:$('#SchNo').val(),VendorName : $('#VendorName').val()},
                    async: false,
                    success: function (result, status, error) {
                         
                        $('#divDashboardGrid').empty().append(result);
                        //GetTotalRows();
                        //LockedFinancialYear();
                    GetTotalRows();
                    }
                });
            }


            $(document).on("click", ".ascCol", function () {
                var $column = $(this).parent().closest("th");
                var colno = $column.data('columnno');
                $('.ascCol').css('color', 'white');
                $('.descCol').css('color', 'white');
                sortTable(colno, true);
                $(this).css('color', 'yellow');
            });

            $(document).on("click", ".descCol", function () {
                var $column = $(this).parent().closest("th");
                var colno = $column.data('columnno');
                $('.ascCol').css('color', 'white');
                $('.descCol').css('color', 'white');
                sortTable(colno, false);
                $(this).css('color', 'yellow');
            });

            function sortTable(columnIndex, ascending) {
                var table = $('#poAppTable');
                var rows = table.find('#divPOAppGrid > tr').toArray();

                rows.sort(function (a, b) {
                    var aValue = $(a).find('td').eq(columnIndex).text();
                    var bValue = $(b).find('td').eq(columnIndex).text();

                    var aIntValue = 0;
                    var bIntValue = 0;
                    if (/^\d+$/.test(aValue) && /^\d+$/.test(bValue)) {
                        aIntValue = parseInt(aValue, 10);
                        bIntValue = parseInt(bValue, 10);
                    }

                    if (aIntValue > 0) {
                        if (ascending) {
                            return aValue - bValue;
                        } else {
                            return bValue - aValue;
                        }
                    }
                    else {
                        if (ascending) {
                            return aValue.localeCompare(bValue);
                        } else {
                            return bValue.localeCompare(aValue);
                        }

                    }

                });
                //if (!ascending) {
                //    rows.reverse();
                //}

                $.each(rows, function (index, row) {
                    table.children('#divPOAppGrid').append(row);
                });
            }

            $('#ApprovalType').change(function () {
                if ($(this).val() == 'finalApproval') {
                    $('#approvalType').val('Approving Final Level');
                    //GetFinalApprovalPO();
                }
                else if ($(this).val() == 'firstLvlApproval') {
                    $('#approvalType').val('Approving First Level');

                    //GetInitialData();
                } else if ($(this).val() == 'finalUnApp') {
                    $('#approvalType').val('Unapproving Final Level');

                    //GetFinalUnApprovalPO();
                } else if ($(this).val() == 'firstLvlUnApp') {
                    $('#approvalType').val('Unapproving first Level');

                    //GetFirstLvlUnApprovalPO();
                } else if ($(this).val() == 'amdUnApp') {
                    $('#approvalType').val('Unapproving Amendment Level');

                    //GetAmmUnApprovalPO();
                }
                else {
                    $('#approvalType').val('Approving Amendment Level');

                    //GetAmmApprovalPO();
                }

                $('#VendorName,#PONo').val('');
            });

            $('#sendToExcel').click(function () {
                $('#poAppTable').table2excel({
                    filename: "PSApprovalTable.xls"
                });
            });

            async function GetTotalRows() {
                 
            var rowCount = $('#divDashboardGrid1 tr').length;
                var columnCount = $('#divDashboardGrid1 tr td').length;

                if (columnCount > 1) {
                    $('#totalRows').text(rowCount);
                }
                else {
                    $('#totalRows').text(0);
                }
            }

        </script>
        <script src="~/Scripts/table2excel.js"></script>
}

<style>
    #poAppTable {
        /*width: 100%;*/
        border-collapse: collapse;
    }

    #poAppThead th {
        background-color: #127387;
        position: sticky;
        top: 0;
        z-index: 1;
    }

    tbody td {
        /* Table cell styles */
    }

    .tooltip-link {
        position: relative;
    }

    .tooltip-link::after {
        content: "SHOW PO";
        position: absolute;
        top: 100%;
        left: 50%;
        transform: translateX(-50%);
        padding: 5px;
        background-color: #333;
        color: #fff;
        border-radius: 3px;
        white-space: nowrap;
        opacity: 0;
        visibility: hidden;
        transition: opacity 0.3s, visibility 0.3s;
    }

    .tooltip-link:hover::after {
        opacity: 1;
        visibility: visible;
    }

    .tooltip-link1 {
        position: relative;
    }

    .tooltip-link1::after {
        content: "SHOW DETAIL";
        position: absolute;
        top: 100%;
        left: 50%;
        transform: translateX(-50%);
        padding: 5px;
        background-color: #333;
        color: #fff;
        border-radius: 3px;
        white-space: nowrap;
        opacity: 0;
        visibility: hidden;
        transition: opacity 0.3s, visibility 0.3s;
    }

    .tooltip-link1:hover::after {
        opacity: 1;
        visibility: visible;
    }

    .sort-icon {
        padding: 5px;
        margin: 5px;
        vertical-align: top;
    }

    .ascCol {
        font-size: 15px;
        vertical-align: bottom;
    }

    .descCol {
        font-size: 15px;
        vertical-align: super;
    }
</style>