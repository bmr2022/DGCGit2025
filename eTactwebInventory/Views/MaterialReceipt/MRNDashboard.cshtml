@model eTactWeb.DOM.Models.MRNQDashboard

@{
    ViewData["Title"] = "Dashboard";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}
<div class="shadow-lg">
    <div class="card rounded border-light bg-gradient">
        <div class="card-header card-headerBG text-light bg-gradient">
            <h5>
                MATERIAL RECEIPT NOTE - <strong>Dashboard</strong>
                <span class="d-flex float-end" style="margin-top:-2px;">
                    <a asp-action="MaterialReceipt" asp-route-ID="0" class="btn btn-sm btn-outline-light bg-gradient">
                        <i class="fa fa-plus-circle fafw fa-lg" aria-hidden="true"></i>
                        Add New
                    </a>
                </span>
            </h5>
        </div>
        <div class="card-body">
            <div class="card overflow-auto">
                <div class="card-body">
                    <form id="SParam">
                        <div class="row">
                            <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
                                <label asp-for="VendorName" class="form-label">Vendor Name</label>
                                <input asp-for="VendorName" class="form-control" />
                                <input type="hidden" asp-for="FromDate1" class="form-control" />
                                <input type="hidden" asp-for="ToDate1" class="form-control" />
                            </div>

                            <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
                                <label asp-for="MrnNo" class="form-label">Mrn No</label>
                                <input asp-for="MrnNo" class="form-control" />
                            </div>
                            <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
                                <label asp-for="GateNo" class="form-label">Gate No</label>
                                <input asp-for="GateNo" class="form-control" />
                            </div>
                            <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
                                <label asp-for="PONo" class="form-label">PO NO</label>
                                <input asp-for="PONo" class="form-control" />
                            </div>
                            <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
                                <label asp-for="ItemName" class="form-label">Item Name</label>
                                <input asp-for="ItemName" class="form-control" />
                            </div>
                            <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
                                <label asp-for="PartCode" class="form-label">Part Code</label>
                                <input asp-for="PartCode" class="form-control" />
                            </div>
                            <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
                                <label asp-for="DashboardType" class="form-label">Type</label>
                                <select asp-for="DashboardType" class="form-select">
                                    <option value="0" disabled>-Select-</option>
                                    <option value="Summary" selected>Summary</option>
                                    <option value="Detail">Detail</option>
                                </select>
                            </div>
                            @* <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12">
                            <label asp-for="ItemName" class="form-label">Item Name</label>


                            <button class="btn btn-sm btn-outline-info" type="button" onclick="GetSearchData();">
                            <i class="fa-solid fa-magnifying-glass fa-fw fa-lg "></i>SEARCH
                            </button>
                            <a class="btn btn-sm btn-outline-danger" asp-controller="PurchaseOrder" asp-action="DashBoard">
                            <i class="fa-solid fa-rotate fa-fw fa-lg"></i>RESET
                            </a>
                            </div>*@
                            @*<div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-12 col-xs-12">
                            <div class="row">
                            <div class="col">
                            <label class="form-label">From Date</label>
                            <input asp-for="FromDate" maxlength="5" class="form-control" />
                            </div>
                            <div class="col">
                            <label class="form-label">To Date</label>
                            <input asp-for="ToDate" maxlength="5" class="form-control" />

                            </div>
                            </div>
                            </div>*@
                            <div class="col-sm-3 col-md-6 col-lg-2 col-xl-4">
                                <div class="row">
                                    <div class="col">
                                        <label class="form-label">From Date</label>
                                        <input asp-for="FromDate" maxlength="10" class="form-control" />
                                    </div>
                                    <div class="col">
                                        <label class="form-label">To Date</label>
                                        <input asp-for="ToDate" maxlength="10" class="form-control" />
                                    </div>
                                </div>
                            </div>
                            <div class="col-sm-3 col-md-6 col-lg-4 col-xl-2">
                                <label class="form-label">Global search</label>
                                <input type="text" class="form-control" id="search-box" style="background-color:#FFFACD" placeholder="Search...">
                            </div>
                            <div class="col-sm-3 col-md-6 col-lg-2 col-xl-4 d-flex justify-content-end mt-3">
                                <button class="btn btn-sm btn-outline-info" type="button" onclick="GetSearchData();">
                                    <i class="fa-solid fa-magnifying-glass fa-fw fa-lg "></i>SEARCH
                                </button>
                                <button class="btn btn-sm btn-outline-success" style="padding:3px;height:30px;width:100px;margin-left:20px;" type="button" id="sendToExcel">
                                    <i class="fa-solid fa-upload fa-fw fa-lg "></i>EXCEL
                                </button>
                                <a class="btn btn-sm btn-outline-danger" asp-controller="MaterialReceipt" asp-action="MRNDashboard" style="padding:3px;height:30px;width:100px;margin-left:20px;">
                                    <i class="fa-solid fa-rotate fa-fw fa-lg"></i>RESET
                                </a>
                            </div>

                            @* <div >
                            <button class="btn btn-sm btn-outline-info" type="button" onclick="GetSearchData();">
                            <i class="fa-solid fa-magnifying-glass fa-fw fa-lg "></i>SEARCH
                            </button>
                            <a class="btn btn-sm btn-outline-danger" asp-controller="PurchaseOrder" asp-action="DashBoard">
                            <i class="fa-solid fa-rotate fa-fw fa-lg"></i>RESET
                            </a>
                            </div>*@
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="card-body pb-0 pt-0">
            <div class="progress" style="height: 4px;">
                <div class="progress-bar progress-bar-striped bg-info" role="progressbar" style="width: 100%" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
            </div>
        </div>
        <div class="card-body">
            <div class="card overflow-auto">
                <div class="card-body" id="divDashboardGrid">
                    <partial name="_MRNDashboardGrid" />
                </div>
            </div>
        </div>
        

    </div>
</div>

@section Scripts
{
    <script>
        $(document).ready(function () {
             
            if ($('#DashboardType').val() == "Summary") {
                //GetFormRights();
                GetSummaryData();
            } else {
                //GetFormRights();
                GetDetailData();
            }
            GetSearchData();
        });

        $(function () {
            GetFormRights();
            var fromDateBack = sessionStorage.getItem('FromDateBack');
            var toDateBack = sessionStorage.getItem('ToDateBack');
            var GateNoBack = sessionStorage.getItem('GateNoBack');
            var VendorNameBack = sessionStorage.getItem('VendorNameBack');
            var PartCodeBack = sessionStorage.getItem('PartCodeBack');
            var ItemNameBack = sessionStorage.getItem('ItemNameBack');
            var MrnNoBack = sessionStorage.getItem('MrnNoBack');
            var PoNoBack = sessionStorage.getItem('PoNoBack');
            var GlobalSearchBack = sessionStorage.getItem('GlobalSearchBack');
            var DashboardTypeBack = sessionStorage.getItem('DashboardTypeBack');

            GetServerDate();
            var currentDate = new Date();

                        var firstDayOfMonth = new Date(currentDate.getFullYear(), currentDate.getMonth(), 1);
            function formatDate(date)
            {
                var day = ("0" + date.getDate()).slice(-2);
                var month = ("0" + (date.getMonth() + 1)).slice(-2);
                var year = date.getFullYear();
                var dateString = day + "/" + month + "/" + year;
                return dateString;

                //GetSearchData();
            }

            var firstDayOfMonth = new Date(currentDate.getFullYear(), currentDate.getMonth(), 1);
                 $('#FromDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", formatDate(firstDayOfMonth));
            $('#ToDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", 'now');
            var dashGrid = $('#mrnTbody tr').length;
            $('#totalRows').text(dashGrid);
           // var currentDate = new Date();

            var sessionYearCode = parseInt($('#SessionYearCode').val().split('/')[2].split(" ")[0]);

            var rowCount = $('#gateDashboardtable tr').length;

            for (var i = 0; i < rowCount; i++) {
                var yearCodeGrid = $('#yearCodeGrid-' + i).val();
                if (sessionYearCode != yearCodeGrid) {
                    $('#editCursorDiv-' + i).prop("disabled", true);
                    $('#deleteCursorDiv-' + i).prop("disabled", true);
                }
            }
        
           var sessionYearCode = parseInt($('#SessionYearCode').val().split('/')[2].split(" ")[0]);
            console.log(sessionYearCode);
            
            //$('#FromDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", 'now');

            //$('#ToDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", 'now');
       
            AutoComplete('VMRNMainDetail', 'VendorName', 'Account_Name', null);
            AutoComplete('VMRNMainDetail', 'ItemName', 'item_name', null);
            AutoComplete('VMRNMainDetail', 'PartCode', 'partcode', null);
            AutoComplete('VMRNMainDetail', 'GateNo', 'GateNo', null);
            AutoComplete('VMRNMainDetail', 'PONo', 'pono', null);
            AutoComplete('VMRNMainDetail', 'MrnNo', 'MRNNo', null);
            //AutoComplete('VMRNMainDetail', 'ItemName', 'ItemName', null);
            if ($('#FromDate1').val() != "" && $('#ToDate1').val() != "") {

                var inputFromDateString = $('#FromDate1').val().split(" ")[0];
                var inputToDateString = $('#ToDate1').val().split(" ")[0];

                var originalFromDate = new Date(inputFromDateString);
                var originalToDate = new Date(inputToDateString);


                //fromdate
                var day = originalFromDate.getDate();
                var month = originalFromDate.getMonth() + 1; // Months are zero-based
                var year = originalFromDate.getFullYear();

                var convertedFromDateText = day.toString().padStart(2, '0') + '/' + month.toString().padStart(2, '0') + '/' + year;

                //todate
                var day = originalToDate.getDate();
                var month = originalToDate.getMonth() + 1; // Months are zero-based
                var year = originalToDate.getFullYear();

                var convertedToDateText = day.toString().padStart(2, '0') + '/' + month.toString().padStart(2, '0') + '/' + year;


                $('#FromDate').val(convertedFromDateText);
                $('#ToDate').val(convertedToDateText);

            }
            if (fromDateBack != null && fromDateBack != '') {

                $('#FromDate').val(fromDateBack);
                $('#ToDate').val(toDateBack);
                $('#GateNo').val(GateNoBack);
                $('#VendorName').val(VendorNameBack);
                $('#PartCode').val(PartCodeBack);
                $('#ItemName').val(ItemNameBack);
                $('#MrnNo').val(MrnNoBack);
                $('#search-box').val(GlobalSearchBack);
                $('#DashboardType').val(DashboardTypeBack);
                $('#PoNo').val(PoNoBack);
            }
           
           
            $("#search-box").val($('#search-box').val()).trigger('keyup');

            sessionStorage.removeItem('FromDateBack');
            sessionStorage.removeItem('ToDateBack');
            sessionStorage.removeItem('GateNoBack');
            sessionStorage.removeItem('VendorNameBack');
            sessionStorage.removeItem('PartCodeBack');
            sessionStorage.removeItem('ItemNameBack');
            sessionStorage.removeItem('MrnNoBack');
            sessionStorage.removeItem('GlobalSearchBack');
            sessionStorage.removeItem('DashboardTypeBack');
            sessionStorage.removeItem('PoNoBack');
        });

        // var currentSearchColumn = null;

        // $("#search-box").on("keyup", function () {
        //     var value = $(this).val().toLowerCase();
        //     $("#mrnTbody tr").filter(function () {
        //         if (currentSearchColumn !== null) {
        //             var columnText = $(this).find('td').eq(currentSearchColumn).text().toLowerCase();
        //             $(this).toggle(columnText.indexOf(value) > -1);
        //         } else {
        //             $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
        //         }
        //     });
        // });


        // $(document).on("click", "th[data-columnno]", function () {
        //     currentSearchColumn = $(this).data('columnno');
        //     $("#search-box").val('').trigger('keyup'); // Reset search box and trigger the search
        // });

          $('#sendToExcel').click(function () {
            var reportType = $('#DashboardType').val(); // assuming there's a dropdown or input with id="ReportType"
            window.location.href = '/MaterialReceipt/ExportMaterialReceiptDataToExcel?ReportType=' + encodeURIComponent(reportType);
        });

        function loadPage(pageNumber) {
            var dashboardType = $('#DashboardType').val();
            var searchString = $('#search-box').val().trim(); // get the current search box value

            let url = '';
            let data = $('#SParam').serialize() + '&pageNumber=' + pageNumber;

            if (searchString !== '') {
                url = '/MaterialReceipt/GlobalSearch';
                data += '&searchString=' + encodeURIComponent(searchString) + '&dashboardType=' + dashboardType;
            } else {
                if (dashboardType === "Summary") {
                    url = '@Url.Action("GetSearchData", "MaterialReceipt")';
                } else if (dashboardType === "Detail") {
                    url = '@Url.Action("MRNDetailDashboard", "MaterialReceipt")';
                }
            }

            $.ajax({
                type: 'GET',
                url: url,
                data: data,
                success: function (result) {
                    $('#divDashboardGrid').empty();
                    $('#divDashboardGrid').html(result);
                    $('#totalRows').text($('#gateDashboardtable tr').length);
                },
                error: function () {
                    console.error("Error loading data.");
                }
            });
        }



        // function loadPage(pageNumber) {

             
        //     var dashboardType = $('#DashboardType').val();
        //     var url = '';
        //     var searchString = $('#search-box').val();
        //     var formData = $('#SParam').serialize();
        //     if (dashboardType === "Summary") {
        //         url = '@Url.Action("GetSearchData", "MaterialReceipt")';
        //     } else if (dashboardType === "Detail") {
        //         url = '@Url.Action("MRNDetailDashboard", "MaterialReceipt")';
        //     }


        //     formData += '&pageNumber=' + pageNumber;

        //     // formData.push({ name: 'pageNumber', value: pageNumber });
        //     console.log("Fromdata ", formData)
        //     $.ajax({
        //         type: 'GET',
        //         url: url,
        //         data: formData,
        //         success: function (result) {
        //             $('#divDashboardGrid').empty();
        //             $('#divDashboardGrid').html(result);
        //             $('#totalRows').text($('#gateDashboardtable tr').length);
        //             //countAllTotal();
        //         }
        //     });
        // }
        var currentSearchColumn = null;
        $("#search-box").on("input", function () {
             
            var searchString = $(this).val().trim();
            var dashboardType = $('#DashboardType').val();
            $.ajax({
                url: "/MaterialReceipt/GlobalSearch?searchString=" + searchString,
                type: "GET",
                data: { searchString: searchString, dashboardType: dashboardType },
                success: function (data) {

                    $('#divDashboardGrid').empty();
                    $('#divDashboardGrid').html(data);
                    $('#totalRows').text($('#gateDashboardtable tr').length);
                    //countAllTotal();
                },
                error: function () {
                    console.log("Error fetching search results.");
                }
            });
        });
        function GetServerDate() {
            $.ajax({
                type: "GET",
                url: "@Url.Action("GetServerDate")",
                async: false,
                success: function (result, status, error) {
                    console.log(result);
                    if (result != null) {
                        // var firstDayOfMonth = getFirstDayOfMonth(result);
                        // $('#FromDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", firstDayOfMonth);
                        $('#ToDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", result);
                    }
                }
            });
        }

        function getFirstDayOfMonth(dateString) {
            const [day, month, year] = dateString.split('/');

            // Create a new Date object (months are 0-indexed, so subtract 1 from the month)
            const currentDate = new Date(year, month - 1, day);

            // Set the date to the 1st day of the month
            currentDate.setDate(1);

            // Format the result as dd/mm/YYYY
            const firstDayOfMonth = `${currentDate.getDate().toString().padStart(2, '0')}/${(currentDate.getMonth() + 1).toString().padStart(2, '0')}/${currentDate.getFullYear()}`;

            return firstDayOfMonth;
        }

        function formatDate(date) {
            var day = ("0" + date.getDate()).slice(-2);
            var month = ("0" + (date.getMonth() + 1)).slice(-2);
            var year = date.getFullYear();
            var dateString = day + "/" + month + "/" + year;
            return dateString;
        }

        function GetSearchData() {
             
            if ($('#DashboardType').val() == "Summary") {
                GetFormRights();
                GetSummaryData();
            }
            else {
                GetFormRights();
                GetDetailData();
            }
        }

        function GetSummaryData() {
            var data = $('#SParam').serialize();
            $.ajax({
                url: '@Url.Action("GetSearchData")',
                type: "POST",
                async: false,
                data: $('#SParam').serializeArray(),
                success: function (data) {
                    $('#divDashboardGrid').empty().append(data);
                    var dashGrid = $('#mrnTbody tr').length;
                    $('#totalRows').text(dashGrid);
                    GetFormRights();
                },
                error: function (response) {
                    alert(response.responseText);
                },
                failure: function (response) {
                    alert(response.responseText);
                }
            });
        }

        function GetDetailData() {
            var data = $('#SParam').serialize();
            $.ajax({
                url: '@Url.Action("MRNDetailDashboard")',
                type: "POST",
                async: false,
                data: $('#SParam').serializeArray(),
                success: function (data) {
                    $('#divDashboardGrid').empty().append(data);
                    var dashGrid = $('#mrnTbody tr').length;
                    $('#totalRows').text(dashGrid);
                    GetFormRights();
                },
                error: function (response) {
                    alert(response.responseText);
                },
                failure: function (response) {
                    alert(response.responseText);
                }
            });
        }

        function GetFormRights() {

            $.ajax({
                url: "@Url.Action("GetFormRights")",
                type: "POST",
                async: false,
                success: function (data) {

                    if (data != null) {
                        var obj = $.parseJSON(data);
                        if (obj.Result.Table.length == 0) {
                            var rowCounter = $('#gateDashboardtable tr').length;
                            for (var i = 0; i < rowCounter; i++) {
                                $('#viewCursorDiv-' + i).empty();
                                $('#viewCursorDiv-' + i).append(`<a class="table-link link-success noCursor" id="viewNoCursor"">
                                                                                                        <span class="fa-stack">
                                                                                                            <i class="fa fa-square fa-stack-2x"></i>
                                                                                                            <i class="fa-solid fa-magnifying-glass fa-stack-1x fa-inverse"></i>
                                                                                                        </span>
                                                                                                    </a>`);
                            }
                            for (var i = 0; i < rowCounter; i++) {
                                $('#editCursorDiv-' + i).empty();
                                $('#editCursorDiv-' + i).append(`<a class="table-link link-primary noCursor">
                                                                                                                <span class="fa-stack">
                                                                                                                    <i class="fa fa-square fa-stack-2x"></i>
                                                                                                                    <i class="fa fa-pencil fa-stack-1x fa-inverse"></i>
                                                                                                                </span>
                                                                                                            </a>`);
                            }
                            for (var i = 0; i < rowCounter; i++) {
                                $('#deleteCursorDiv-' + i).empty();
                                $('#deleteCursorDiv-' + i).append(`<a class="table-link link-danger noCursor">
                                                                                                                <span class="fa-stack">
                                                                                                                    <i class="fa fa-square fa-stack-2x"></i>
                                                                                                                    <i class="fa-solid fa-trash fa-stack-1x fa-inverse"></i>
                                                                                                                </span>
                                                                                                            </a>`);
                            }
                            $("#divDashboardGrid").css("display", "none");
                            $(".totalrows").css("display", "none");
                            $("#SParam").hide();
                        } else {

                            var optAll = obj.Result.Table[0].OptAll;
                            var optSave = obj.Result.Table[0].OptSave;
                            var optUpdate = obj.Result.Table[0].OptUpdate;
                            var optDelete = obj.Result.Table[0].OptDelete;
                            var optView = obj.Result.Table[0].OptView;
                            var rowCounter = $('#gateDashboardtable tr').length;
                            if (!optAll) {
                                if (!optSave) {

                                    $("#savebtn").css("pointer-events", "none");
                                } else {
                                    $("#savebtn").css("pointer-events", "");
                                }
                                if (!optView) {

                                    for (var i = 0; i < rowCounter; i++) {
                                        $('#viewCursorDiv-' + i).empty();
                                        $('#viewCursorDiv-' + i).append(`<a class="table-link link-success noCursor" id="viewNoCursor"">
                                                                        <span class="fa-stack">
                                                                            <i class="fa fa-square fa-stack-2x"></i>
                                                                            <i class="fa-solid fa-magnifying-glass fa-stack-1x fa-inverse"></i>
                                                                        </span>
                                                                    </a>`);
                                    }
                                }
                                if (!optUpdate) {
                                    for (var i = 0; i < rowCounter; i++) {
                                        $('#editCursorDiv-' + i).empty();
                                        $('#editCursorDiv-' + i).append(`<a class="table-link link-primary noCursor">
                                                                        <span class="fa-stack">
                                                                            <i class="fa fa-square fa-stack-2x"></i>
                                                                            <i class="fa fa-pencil fa-stack-1x fa-inverse"></i>
                                                                        </span>
                                                                    </a>`);
                                    }
                                }
                                if (!optDelete) {
                                    for (var i = 0; i < rowCounter; i++) {
                                        $('#deleteCursorDiv-' + i).empty();
                                        $('#deleteCursorDiv-' + i).append(`<a class="table-link link-danger noCursor">
                                                                        <span class="fa-stack">
                                                                            <i class="fa fa-square fa-stack-2x"></i>
                                                                            <i class="fa-solid fa-trash fa-stack-1x fa-inverse"></i>
                                                                        </span>
                                                                    </a>`);
                                    }
                                }
                                if (!(optUpdate || optDelete || optView)) {
                                    $("#divDashboardGrid").css("display", "none");
                                    $(".totalrows").css("display", "none");
                                    $("#SParam").hide();
                                }
                            }
                        }
                    }
                },
                error: function (errMsg) {
                    $.notify(errMsg, { position: "top center", className: "error" });
                }
            });
        }

        // $('#sendToExcel').click(function () {
        //     $("#MRNTable").table2excel({
        //         filename: "MRNData.xls"
        //     });
        // });


      

        $(document).on("click", ".sortCol", function () {
             ;
            var $column = $(this).parent().closest("th");
            var colno = $(this).data('columnno');
            var currentOrder = $(this).data('sortorder');

            $('.sortCol').css('color', 'white');

            if (currentOrder === 'asc') {
                sortTable(colno, false);
                $(this).data('sortorder', 'desc');
                $(this).css('color', 'yellow'); // Visual indication of descending order
                //$(this).css('font-size', 'larger'); // Visual indication of descending order
                $(this).find('i:first').removeClass('fa-arrow-up');
                $(this).find('i:first').addClass('fa-arrow-down');
            } else {
                sortTable(colno, true);
                $(this).data('sortorder', 'asc');
                $(this).css('color', 'yellow');
                // $(this).css('font-size', 'larger'); // Visual indication of ascending order
                $(this).find('i:first').removeClass('fa-arrow-down');
                $(this).find('i:first').addClass('fa-arrow-up');
            }
        });

        function sortTable(columnIndex, ascending) {

            var table = $('#MRNTable');
            var rows = table.find('#mrnTbody > tr').toArray();

            rows.sort(function (a, b) {
                var aValue = $(a).find('td').eq(columnIndex).text();
                var bValue = $(b).find('td').eq(columnIndex).text();

                var aIntValue = 0;
                var bIntValue = 0;
                if (/^\d*\.?\d+$/.test(aValue) && /^\d*\.?\d+$/.test(bValue)) {
                    aIntValue = parseInt(aValue, 10);
                    bIntValue = parseInt(bValue, 10);
                }

                if (aIntValue > 0) {
                    if (ascending) {
                        return aValue - bValue;
                    } else {
                        return bValue - aValue;
                    }
                } else {
                    if (ascending) {
                        return aValue.localeCompare(bValue);
                    } else {
                        return bValue.localeCompare(aValue);
                    }
                }
            });

            $.each(rows, function (index, row) {
                table.children('#mrnTbody').append(row);
            });
        }

        function confirmEdit(event, MrnNo, YearCode) {

            event.preventDefault();
            var urlHref = $(event.currentTarget).attr("href");
            CheckEditOrDelete(urlHref, MrnNo, YearCode);
        }

        function confirmDelete(event, MrnNo, YearCode) {

            event.preventDefault();
            var urlHref = $(event.currentTarget).attr("href");

            if (confirm("Are you sure you want to delete this entry?")) {
                CheckEditOrDelete(urlHref, MrnNo, YearCode);
            }
        }

        function BackButtonData(event, MrnNo, YearCode) {

            event.preventDefault();
            var urlHref = $(event.currentTarget).attr("href");
            var linkUrl = urlHref;
            linkUrl = linkUrl + "&FromDate=" + $('#FromDate').val() + "&ToDate=" + $('#ToDate').val() + "&VendorName=" + $('#VendorName').val() + "&MrnNo=" + $('#MrnNo').val() + "&GateNo=" + $('#GateNo').val() + "&PoNo=" + $('#PONo').val() + "&ItemName=" + $('#ItemName').val() + "&PartCode=" + $('#PartCode').val() + "&Type=" + $('#DashboardType').val() + "&Searchbox=" + $('#search-box').val();
            window.location.href = linkUrl;
        }

        function CheckEditOrDelete(urlHref, MrnNo, YearCode) {

            event.preventDefault();
            $.ajax({
                url: '@Url.Action("CheckEditOrDelete")',
                type: "POST",
                data: { "MRNNo": MrnNo, "YearCode": YearCode },
                async: false,
                success: function (data) {

                    var obj = $.parseJSON(data);
                    if (obj.Result.length > 0) {
                        if (obj.Result[0].MIRNO == '') {
                            var linkUrl = urlHref;
                             
                            linkUrl = linkUrl + "&FromDate=" + $('#FromDate').val() + "&ToDate=" + $('#ToDate').val() + "&VendorName=" + $('#VendorName').val() + "&MrnNo=" + $('#MrnNo').val() + "&GateNo=" + $('#GateNo').val() + "&PoNo=" + $('#PONo').val() + "&ItemName=" + $('#ItemName').val() + "&PartCode=" + $('#PartCode').val() + "&Type=" + $('#DashboardType').val() + "&Searchbox=" + $('#search-box').val();
                            window.location.href = linkUrl;
                        }
                        else {
                            var MirNo = obj.Result[0].MIRNO;
                            $.notify("Cannot edit/delete this item as QC with MirNo : " + MirNo + " already exists", "error");
                            return false;
                        }
                    }
                    else {
                        var linkUrl = urlHref;
                        linkUrl = linkUrl + "&FromDate=" + $('#FromDate').val() + "&ToDate=" + $('#ToDate').val() + "&VendorName=" + $('#VendorName').val() + "&MrnNo=" + $('#MrnNo').val() + "&GateNo=" + $('#GateNo').val() + "&PoNo=" + $('#PONo').val() + "&ItemName=" + $('#ItemName').val() + "&PartCode=" + $('#PartCode').val() + "&Type=" + $('#DashboardType').val();
                        window.location.href = linkUrl;
                    }

                },
                error: function (response) {
                    alert(response.responseText);
                },
                failure: function (response) {
                    alert(response.responseText);
                }
            });
        }

        function selection(CurrentRow) {

            $(CurrentRow).css({ 'background-color': '#00bcd4' });
            $("#mrnTbody tr").not(CurrentRow).css({ 'background-color': '' });
        }
    </script>
    <script src="~/Scripts/table2excel.js"></script>

}