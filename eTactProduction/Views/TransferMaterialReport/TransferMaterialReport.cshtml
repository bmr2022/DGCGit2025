@model eTactWeb.DOM.Models.TransferMaterialReportModel

@{
    ViewData["Title"] = "Transfer Material Report";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}

<div class="card bg-gradient mb-3 border-light shadow-lg">

    <div class="card-header card-headerBG text-light bg-gradient">
        <h5>
            <strong>Transfer Material Report</strong>
            <span class="d-flex float-end" style="margin-top:-2px;">
                @*<a asp-action="Dashboard" asp-route-ID="0" class="btn btn-sm btn-outline-light bg-gradient">*@

            </span>
        </h5>
    </div>
    <div>
    </div>
    <div class="card-body bg-light pt-0">
        <div class="accordion shadow-lg">
            <div class="card-body">
                <div class="card overflow-auto">
                    <div class="card-body">
                        <form id="SParam">
                            <div class="row">
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-12 col-xs-12">
                                    <div class="row">
                                        <div class="col">
                                            <label class="form-label">From Date</label>
                                            <input asp-for="FromDate" class="form-control" />
                                        </div>
                                        <div class="col">
                                            <label class="form-label">To Date</label>
                                            <input asp-for="ToDate" class="form-control" />
                                        </div>
                                    </div>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" style="width:150px">
                                    <label asp-for="FromWorkCenter" class="form-label">From WorkCenter</label>
                                    <select asp-for="FromWorkCenter" class="form-select"></select>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" style="width:150px">
                                    <label asp-for="ToWorkCenter" class="form-label">To WorkCenter</label>
                                    <select asp-for="ToWorkCenter" class="form-select"></select>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" style="width:150px">
                                    <label asp-for="Tostore" class="form-label">To Store</label>
                                    <select asp-for="Tostore" class="form-select"></select>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" style="width:200px">
                                    <label asp-for="PartCode" class="form-label">PartCode</label>
                                    <input type="hidden" asp-for="ItemCode" value="@Model.ItemCode" />
                                    <select asp-for="PartCode" class="form-select">
                                        <option value="@Model.ItemCode">-Select-</option>
                                    </select>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" style="width:200px">
                                    <label asp-for="ItemName" class="form-label">ItemName</label>
                                    <input type="hidden" asp-for="ItemCode" value="@Model.ItemCode" />
                                    <select asp-for="ItemName" class="form-select">
                                        <option value="@Model.ItemCode">-Select-</option>
                                    </select>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" style="width:150px">
                                    <label asp-for="TransferSlipNo" class="form-label">Transfer SlipNo</label>
                                    <select asp-for="TransferSlipNo" class="form-select"></select>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" style="width:150px">
                                    <label asp-for="ProdSlipNo" class="form-label">Prod SlipNo</label>
                                    <select asp-for="ProdSlipNo" class="form-select"></select>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" style="width:150px">
                                    <label asp-for="ProdPlanNo" class="form-label">Prod PlanNo</label>
                                    <select asp-for="ProdPlanNo" class="form-select"></select>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" style="width:150px">
                                    <label asp-for="ProdSchNo" class="form-label">Prod SchNo</label>
                                    <select asp-for="ProdSchNo" class="form-select"></select>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" style="width:150px">
                                    <label asp-for="ProcessName" class="form-label">Process Name</label>
                                    <select asp-for="ProcessName" class="form-select"></select>
                                </div>
                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12">
                                    <label asp-for="ReportType" class="form-label">Report Type</label>
                                    <select asp-for="ReportType" class="form-select" onchange="GetTransferMaterialReport()">
                                        <option disabled value="">-Select-</option>
                                        <option value="TransferMaterial Deatil">TransferMaterial Deatil</option>
                                        <option value="TransferMaterial Date Wise Summary">TransferMaterial Date Wise Summary</option>
                                        <option selected value="Detail">Detail</option>
                                        <option value="FromWcAndItemWiseConsolidated">From Wc And ItemWise Consolidated</option>
                                        <option value="FromWcAndItem+DateWiseConsolidated">From Wc And Item+DateWise Consolidated</option>
                                        
                                    </select>
                                </div>
                                <div class="col d-flex align-items-center">
                                    <div class="me-2">
                                        <label class="form-label">Global search</label>
                                        <input id="search-box" class="form-control" style="background-color: #FFFACD;" placeholder="Search..." />
                                    </div>
                                    <button class="btn btn-sm btn-outline-info me-2" type="button" style="padding:0px;height:30px;width:100px;" onclick="GetTransferMaterialReport();">
                                        <i class="fa-solid fa-magnifying-glass fa-fw fa-lg"></i>SEARCH
                                    </button>
                                    <button class="btn btn-sm btn-outline-success me-2" style="padding:3px;height:30px;width:100px;" type="button" id="sendToExcel">
                                        <i class="fa-solid fa-upload fa-fw fa-lg"></i>EXCEL
                                    </button>
                                    <a class="btn btn-sm btn-outline-danger" style="padding:3px;height:30px;width:100px;" asp-controller="TransferMaterialReport" asp-action="TransferMaterialReport">
                                        <i class="fa-solid fa-rotate fa-fw fa-lg"></i>RESET
                                    </a>
                                </div>
                            </div>
                            <div class="progress my-2 mb-3" style="height: 4px;">
                                <div class="progress-bar progress-bar-striped bg-secondary progress-bar-animated" role="progressbar" style="width: 100%" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
                            </div>
                            <div class="accordion-item">
                                <h2 class="accordion-header" id="H_SAGrid">
                                    <button class="accordion-button collapsed text-light pnl4BG bg-gradient" type="button" data-bs-toggle="collapse" data-bs-target="#B_SAGrid" aria-expanded="true" aria-controls="B_SAGrid">
                                        <strong style="text-transform:uppercase"><i class="fa-solid fa-table-list fa-lg fa-fw pr35 fa-fade" style="--fa-animation-duration: 2s; --fa-fade-opacity: 0.6;"></i>Transfer Material Report Grid</strong>
                                    </button>
                                </h2>
                                <div id="B_SAGrid" class="accordion-collapse collapse pnl4 bg-gradient show" aria-labelledby="H_SAGrid">
                                    <div class="accordion-body">
                                        <div class="card-body" id="divPendingMRP">
                                            <div class="row">
                                                <div class="col-md-12" id="divTransferMaterialGrid">
                                                    <partial name="_TransferMaterialReportGrid" />
                                                </div>
                                            </div>
                                        </div>
                                        <center>
                                            <div class="row">
                                               @*  <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" id="prodschDiv">
                                                    <label class="form-label">Prod Sch Qty</label>
                                                    <input id="TotalProdSchQty" class="form-control" readonly />
                                                </div>
                                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" id="prodqtyDiv">
                                                    <label class="form-label">Prod Qty</label>
                                                    <input id="TotalProdQty" class="form-control" readonly />
                                                </div>
                                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" id="okqtyDiv">
                                                    <label class="form-label">Ok Qty</label>
                                                    <input id="TotalOkQty" class="form-control" readonly />
                                                </div> *@
                                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" id="transferDiv">
                                                    <label class="form-label">Tranfer Qty</label>
                                                    <input id="TotalTransferQty" class="form-control" readonly />
                                                </div>
                                                <div class="col-xxl-2 col-xl-3 col-lg-3 col-md-6 col-sm-6 col-xs-12" id="altTransferDiv">
                                                    <label class="form-label">Alt TransferQty</label>
                                                    <input id="TotalAltTransferQty" class="form-control" readonly />
                                                </div>
                                            </div>
                                        </center>
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    <script>
        $(document).ready(function () {
            GetServerDate();
            FillFromWorkCenter();
            FillToWorkCenter();
            FillToStore();
            FillPartCode();
            FillItemName();
            FillTransferSlipNo();
            FillProdSlipNo();
            FillProdPlanNo();
            FillProdSchNo();
            FillProcessName();
            //GetTransferMaterialReport();
            $('#PartCode').select2();
            $('#ItemName').select2();
            $('#FromWorkCenter').select2();
            $('#ToWorkCenter').select2();
            $('#Tostore').select2();
            $('#TransferSlipNo').select2();
            $('#TransferTowc').select2();
            $('#ReportType').select2();
            $('#ProdSlipNo').select2();
            $('#ProdPlanNo').select2();
            $('#ProdSchNo').select2();
            $('#ProcessName').select2();
        });

        $('#FromDate').change(function () {
            FillFromWorkCenter();
            FillToWorkCenter();
            FillToStore();
            FillPartCode();
            FillItemName();
            FillTransferSlipNo();
            FillProdSlipNo();
            FillProdPlanNo();
            FillProdSchNo();
            FillProcessName();
        })

        $('#ToDate').change(function () {
            FillFromWorkCenter();
            FillToWorkCenter();
            FillToStore();
            FillPartCode();
            FillItemName();
            FillTransferSlipNo();
            FillProdSlipNo();
            FillProdPlanNo();
            FillProdSchNo();
            FillProcessName();
        })
        $('#ItemName').change(function () {
             
            $('#ItemCode').val($('#ItemName').val());
               
        }); 
        $('#PartCode').change(function () {
            $('#ItemCode').val($('#PartCode').val());
               
        });
        function HideShow() {

            if ($('#ReportType').val() == 'Detail') {
                $('#transferDiv').show();
                $('#altTransferDiv').show();
            }
            if ($('#ReportType').val() == 'FromWcAndItemWiseConsolidated' || $('#ReportType').val() == 'FromWcAndItem+DateWiseConsolidated' ) {
                $('#transferDiv').show();
                $('#altTransferDiv').show();
            }
        }

        function GetServerDate() {
            $.ajax({
                type: "GET",
                url: "@Url.Action("GetServerDate")",
                async: false,
                success: function (result, status, error) {
                    if (result != null) {
                        var firstDayOfMonth = getFirstDayOfMonth(result);
                        $('#FromDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", firstDayOfMonth);
                        $('#ToDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", result);

                    }
                }
            });
        }

        function getFirstDayOfMonth(dateString) {

            const [day, month, year] = dateString.split('/');
            const currentDate = new Date(year, month - 1, day);
            currentDate.setDate(1);
            const firstDayOfMonth = `${currentDate.getDate().toString().padStart(2, '0')}/${(currentDate.getMonth() + 1).toString().padStart(2, '0')}/${currentDate.getFullYear()}`;
            return firstDayOfMonth;
        }

        function formatDate(dateStr) {
            const months = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];
            const [day, month, year] = dateStr.split('/');
            const monthName = months[parseInt(month) - 1];
            return `${day}/${monthName}/${year}`;
        }

        function GetTransferMaterialReport() {

            $.ajax({
                type: "POST",
                url: "@Url.Action("GetTransferMaterialReport")",
                async: false,
                data: { "ReportType": $('#ReportType').val(), "FromDate": $('#FromDate').val(), "ToDate": $('#ToDate').val(), "FromWorkCenter": $('#FromWorkCenter').val(), "ToWorkCenter": $('#ToWorkCenter').val(), "Tostore": $('#Tostore').val(), "PartCode": $('#ItemCode').val(), "ItemName": $('#ItemCode').val(), "TransferSlipNo": $('#TransferSlipNo').val(), "ProdSlipNo": $('#ProdSlipNo').val(), "ProdPlanNo": $('#ProdPlanNo').val(), "ProdSchNo": $('#ProdSchNo').val(), "ProcessName": $('#ProcessName').val() },
                success: function (result, status, error) {
                    $('#divTransferMaterialGrid').empty();
                    $('#divTransferMaterialGrid').html(result);
                    $('#totalRows').text($('#divTransferMaterialGrid tr').length - 1);
                    countAllTotal();
                    HideShow();
                }
            });
        }

        function DeleteRow(i) {

            var indexToRemove = i - 1;
            $('#SAGridRow-' + i).remove();
            $('#ReportTable tbody tr').each(function (index) {
                $(this).attr('id', 'SAGridRow-' + (index + 1));

                $(this).find('td:first-child a:first-child').each(function (tdIndex) {
                    $(this).attr('id', (index + 1));
                });
                $(this).find('td[id^="opnStk-"]').each(function (tdIndex) {
                    $(this).attr('id', 'opnStk-' + (index + 1));
                });
                $(this).find('td[id^="totStk-"]').each(function (tdIndex) {
                    $(this).attr('id', 'totStk-' + (index + 1));
                });
                $(this).find('td[id^="recQty-"]').each(function (tdIndex) {
                    $(this).attr('id', 'recQty-' + (index + 1));
                });
                $(this).find('td[id^="issQty-"]').each(function (tdIndex) {
                    $(this).attr('id', 'issQty-' + (index + 1));
                });
                $(this).find('td[id^="rate-"]').each(function (tdIndex) {
                    $(this).attr('id', 'rate-' + (index + 1));
                });
                $(this).find('td[id^="amount-"]').each(function (tdIndex) {
                    $(this).attr('id', 'amount-' + (index + 1));
                });

            });
            //$('#ReportTable tr:eq(' + indexToRemove + ')').remove();
            countAllTotal();
            $('#totalRows').text($('#divStockRegisterBody tr').length - 1);
        }

        // function countAllTotal() {

        //     var rowCount = $('#divStockRegisterBody tr').length;
        //     var TransferQty = 0;
        //     var AltTransferQty = 0;
        //     var OkQty = 0;
        //     var RejQty = 0;
        //     var RmConsQty = 0;
        //     for (var i = 1; i <= rowCount; i++) {
        //         TransferQty += $('#TransferQty-' + i).text() == '' ? 0 : parseFloat($('#TransferQty-' + i).text());
        //         AltTransferQty += $('#AltTransferQty-' + i).text() == '' ? 0 : parseFloat($('#AltTransferQty-' + i).text());
        //         OkQty += $('#OkQty-' + i).text() == '' ? 0 : parseFloat($('#OkQty-' + i).text());
        //         RejQty += $('#RejQty-' + i).text() == '' ? 0 : parseFloat($('#RejQty-' + i).text());
        //         RmConsQty += $('#RMConsQty-' + i).text() == '' ? 0 : parseFloat($('#RMConsQty-' + i).text());
        //     }

        //     $('#TotalTransferQty').val(isNaN(TransferQty) ? 0 : TransferQty);
        //     $('#TotalAltTransferQty').val(isNaN(AltTransferQty) ? 0 : AltTransferQty);
        //     //$('#TotalAltTransferQty').val(isNaN(OkQty) ? 0 : OkQty);
        //     $('#TotalRejQty').val(isNaN(RejQty) ? 0 : RejQty);
        //     $('#TotalRMConsQty').val(isNaN(RmConsQty) ? 0 : RmConsQty);
        // }

        function countAllTotal() {
             
            var TransferQty = 0;
            var AltTransferQty = 0;
            var OkQty = 0;
            var RejQty = 0;
            var RmConsQty = 0;

            $('[id^="TransferQty-"]').each(function () {
                var val = parseFloat($(this).text());
                TransferQty += isNaN(val) ? 0 : val;
            });

            $('[id^="AltTransferQty-"]').each(function () {
                var val = parseFloat($(this).text());
                AltTransferQty += isNaN(val) ? 0 : val;
            });

            $('[id^="OkQty-"]').each(function () {
                var val = parseFloat($(this).text());
                OkQty += isNaN(val) ? 0 : val;
            });

            $('[id^="RejQty-"]').each(function () {
                var val = parseFloat($(this).text());
                RejQty += isNaN(val) ? 0 : val;
            });

            $('[id^="RMConsQty-"]').each(function () {
                var val = parseFloat($(this).text());
                RmConsQty += isNaN(val) ? 0 : val;
            });

            $('#TotalTransferQty').val(TransferQty);
            $('#TotalAltTransferQty').val(AltTransferQty);
            $('#TotalRejQty').val(RejQty);
            $('#TotalRMConsQty').val(RmConsQty);
        }


        function FillFromWorkCenter() {
             
            $.ajax({
                type: "POST",
                url: "@Url.Action("FillFromWorkCenter")",
                dataType: "JSON",
                data: { "FromDate": formatDate($('#FromDate').val()), "ToDate": formatDate($('#ToDate').val()) },
                async: false,
                success: function (result, status, error) {
                     
                    if (result != null) {
                        var obj = $.parseJSON(result);
                        if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
                            $('#FromWorkCenter').empty();
                            $('#FromWorkCenter').append('<option value="0" selected>-Select-</option>');
                            $.each(obj.Result, function (index, val) {
                                $('#FromWorkCenter').append('<option value="' + val.TransferFromWC +
                                    '">' + val.TransferFromWC + '</option>');
                            });
                        }
                    }
                }
            });
        }

        function FillToWorkCenter() {
             
            $.ajax({
                type: "POST",
                url: "@Url.Action("FillToWorkCenter")",
                dataType: "JSON",
                data: { "FromDate": formatDate($('#FromDate').val()), "ToDate": formatDate($('#ToDate').val()) },
                async: false,
                success: function (result, status, error) {
                     
                    if (result != null) {
                        var obj = $.parseJSON(result);
                        if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
                            $('#ToWorkCenter').empty();
                            $('#ToWorkCenter').append('<option value="0" selected>-Select-</option>');
                            $.each(obj.Result, function (index, val) {
                                $('#ToWorkCenter').append('<option value="' + val.TransferToWC +
                                    '">' + val.TransferToWC + '</option>');
                            });
                        }
                    }
                }
            });
        }

        function FillToStore() {
             
            $.ajax({
                type: "POST",
                url: "@Url.Action("FillToStore")",
                dataType: "JSON",
                data: { "FromDate": formatDate($('#FromDate').val()), "ToDate": formatDate($('#ToDate').val()) },
                async: false,
                success: function (result, status, error) {
                     
                    if (result != null) {
                        var obj = $.parseJSON(result);
                        if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
                            $('#Tostore').empty();
                            $('#Tostore').append('<option value="0" selected>-Select-</option>');
                            $.each(obj.Result, function (index, val) {
                                $('#Tostore').append('<option value="' + val.TransferToStore +
                                    '">' + val.TransferToStore + '</option>');
                            });
                        }
                    }
                }
            });
        }

        function FillPartCode() {
             
            $.ajax({
                type: "POST",
                url: "@Url.Action("FillPartCode")",
                dataType: "JSON",
                data: { "FromDate": formatDate($('#FromDate').val()), "ToDate": formatDate($('#ToDate').val()) },
                async: false,
                success: function (result, status, error) {
                     
                    if (result != null) {
                        var obj = $.parseJSON(result);
                        if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
                            $('#PartCode').empty();
                            $('#PartCode').append('<option value="0" selected>-Select-</option>');
                            $.each(obj.Result, function (index, val) {
                                $('#PartCode').append('<option value="' + val.ItemCode +
                                    '">' + val.PartCode + '</option>');
                            });
                        }
                    }
                }
            });
        }

        function FillItemName() {
             
            $.ajax({
                type: "POST",
                url: "@Url.Action("FillItemName")",
                dataType: "JSON",
                data: { "FromDate": formatDate($('#FromDate').val()), "ToDate": formatDate($('#ToDate').val()) },
                async: false,
                success: function (result, status, error) {
                     
                    if (result != null) {
                        var obj = $.parseJSON(result);
                        if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
                            $('#ItemName').empty();
                            $('#ItemName').append('<option value="0" selected>-Select-</option>');
                            $.each(obj.Result, function (index, val) {
                                $('#ItemName').append('<option value="' + val.ItemCode +
                                    '">' + val.ItemName + '</option>');
                            });
                        }
                    }
                }
            });
        }
        $(document).on('select2:select', '#ItemName,#PartCode', function (e) {
            var cntId = e.target.id;

            if (cntId == 'ItemName') {
                ItemNameChange(cntId);
            }
            else {
                PartCodeChange(cntId);
            }
        });

        function ItemNameChange(ID) {
            SetPartCodeItemName(ID);
        }

        function PartCodeChange(ID) {
            $.when(SetPartCodeItemName(ID)).done(function () {
            });
        }

        function SetPartCodeItemName(ID) {
            if (!ID) return;
            else {

                var v = $('#' + ID).select2('val');
                if (ID == "PartCode") {
                    $("#ItemName").val(v).trigger("change");
                }
                if (ID == "ItemName") {
                    $("#PartCode").val(v).trigger("change");
                }
            }
        }

        function FillTransferSlipNo() {
             
            $.ajax({
                type: "POST",
                url: "@Url.Action("FillTransferSlipNo")",
                dataType: "JSON",
                data: { "FromDate": formatDate($('#FromDate').val()), "ToDate": formatDate($('#ToDate').val()) },
                async: false,
                success: function (result, status, error) {
                     
                    if (result != null) {
                        var obj = $.parseJSON(result);
                        if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
                            $('#TransferSlipNo').empty();
                            $('#TransferSlipNo').append('<option value="0" selected>-Select-</option>');
                            $.each(obj.Result, function (index, val) {
                                $('#TransferSlipNo').append('<option value="' + val.TransferMatSlipNo +
                                    '">' + val.TransferMatSlipNo + '</option>');
                            });
                        }
                    }
                }
            });
        }

        function FillProdSlipNo() {
             
            $.ajax({
                type: "POST",
                url: "@Url.Action("FillProdSlipNo")",
                dataType: "JSON",
                data: { "FromDate": formatDate($('#FromDate').val()), "ToDate": formatDate($('#ToDate').val()) },
                async: false,
                success: function (result, status, error) {
                     
                    if (result != null) {
                        var obj = $.parseJSON(result);
                        if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
                            $('#ProdSlipNo').empty();
                            $('#ProdSlipNo').append('<option value="0" selected>-Select-</option>');
                            $.each(obj.Result, function (index, val) {
                                $('#ProdSlipNo').append('<option value="' + val.ProdSlipNo +
                                    '">' + val.ProdSlipNo + '</option>');
                            });
                        }
                    }
                }
            });
        }

        function FillProdPlanNo() {
             
            $.ajax({
                type: "POST",
                url: "@Url.Action("FillProdPlanNo")",
                dataType: "JSON",
                data: { "FromDate": formatDate($('#FromDate').val()), "ToDate": formatDate($('#ToDate').val()) },
                async: false,
                success: function (result, status, error) {
                     
                    if (result != null) {
                        var obj = $.parseJSON(result);
                        if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
                            $('#ProdPlanNo').empty();
                            $('#ProdPlanNo').append('<option value="0" selected>-Select-</option>');
                            $.each(obj.Result, function (index, val) {
                                $('#ProdPlanNo').append('<option value="' + val.ProdPlanNo +
                                    '">' + val.ProdPlanNo + '</option>');
                            });
                        }
                    }
                }
            });
        }

        function FillProdSchNo() {
             
            $.ajax({
                type: "POST",
                url: "@Url.Action("FillProdSchNo")",
                dataType: "JSON",
                data: { "FromDate": formatDate($('#FromDate').val()), "ToDate": formatDate($('#ToDate').val()) },
                async: false,
                success: function (result, status, error) {
                     
                    if (result != null) {
                        var obj = $.parseJSON(result);
                        if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
                            $('#ProdSchNo').empty();
                            $('#ProdSchNo').append('<option value="0" selected>-Select-</option>');
                            $.each(obj.Result, function (index, val) {
                                $('#ProdSchNo').append('<option value="' + val.ProdSchNo +
                                    '">' + val.ProdSchNo + '</option>');
                            });
                        }
                    }
                }
            });
        }

        function FillProcessName() {
             
            $.ajax({
                type: "POST",
                url: "@Url.Action("FillProcessName")",
                dataType: "JSON",
                data: { "FromDate": formatDate($('#FromDate').val()), "ToDate": formatDate($('#ToDate').val()) },
                async: false,
                success: function (result, status, error) {
                     
                    if (result != null) {
                        var obj = $.parseJSON(result);
                        if (obj.StatusCode == 200 && obj.StatusText == 'Success') {
                            $('#ProcessName').empty();
                            $('#ProcessName').append('<option value="0" selected>-Select-</option>');
                            $.each(obj.Result, function (index, val) {
                                $('#ProcessName').append('<option value="' + val.StageDescription +
                                    '">' + val.StageDescription + '</option>');
                            });
                        }
                    }
                }
            });
        }

        $('#sendToExcel').click(function () {
            let table = $("#ReportTable");
            TableToExcel.convert(table[0], {
                name: `TransferReportTable.xlsx`,
                sheet: {
                    name: 'TransferReportTable'
                }
            });
        });

        var currentSearchColumn = null;
        $(document).on("click", "th[data-columnno]", function () {
            currentSearchColumn = $(this).data('columnno');
            $("#search-box").val('').trigger('keyup'); // Reset search box and trigger the search
        });
        $("#search-box").on("keyup", function () {
            var value = $(this).val().toLowerCase();
            $("#divStockRegisterBody tr").filter(function () {
                if (currentSearchColumn !== null) {
                    var columnText = $(this).find('td').eq(currentSearchColumn).text().toLowerCase();
                    $(this).toggle(columnText.indexOf(value) > -1);
                } else {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
                }
            });
        });

        $(document).on("click", ".sortCol", function () {
            var $column = $(this).parent().closest("th");
            var colno = $(this).data('columnno');
            var currentOrder = $(this).data('sortorder');

            $('.sortCol').css('color', 'white');

            if (currentOrder === 'asc') {
                sortTable(colno, false);
                $(this).data('sortorder', 'desc');
                $(this).css('color', 'yellow'); // Visual indication of descending order
                //$(this).css('font-size', 'larger'); // Visual indication of descending order
                $(this).find('i:first').removeClass('fa-arrow-up');
                $(this).find('i:first').addClass('fa-arrow-down');
            } else {
                sortTable(colno, true);
                $(this).data('sortorder', 'asc');
                $(this).css('color', 'yellow');
                // $(this).css('font-size', 'larger'); // Visual indication of ascending order
                $(this).find('i:first').removeClass('fa-arrow-down');
                $(this).find('i:first').addClass('fa-arrow-up');
            }
        });

        function sortTable(columnIndex, ascending) {

            var table = $('#ReportTable');
            var rows = table.find('#divStockRegisterBody > tr').toArray();

            rows.sort(function (a, b) {
                var aValue = $(a).find('td').eq(columnIndex).text();
                var bValue = $(b).find('td').eq(columnIndex).text();

                var aIntValue = 0;
                var bIntValue = 0;
                if (/^\d+$/.test(aValue) && /^\d+$/.test(bValue)) {
                    aIntValue = parseInt(aValue, 10);
                    bIntValue = parseInt(bValue, 10);
                }

                if (aIntValue > 0) {
                    if (ascending) {
                        return aValue - bValue;
                    } else {
                        return bValue - aValue;
                    }
                } else {
                    if (ascending) {
                        return aValue.localeCompare(bValue);
                    } else {
                        return bValue.localeCompare(aValue);
                    }
                }
            });

            $.each(rows, function (index, row) {
                table.children('#divStockRegisterBody').append(row);
            });
        }

    </script>
    <script src="https://cdn.jsdelivr.net/gh/linways/table-to-excel@v1.0.4/dist/tableToExcel.js"></script>
}