@model eTactWeb.DOM.Models.HRAListDataModel

@{
    ViewData["Title"] = "HR Pend Attendance";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}


<div class="shadow-lg">
    <div class="card rounded border-light bg-gradient">
        <div class="card-header card-headerBG text-light bg-gradient">
            <h5>
                HR Attendance - <strong>Pend Attendance</strong>
                <span class="d-flex float-end" style="margin-top:-2px;">
                    <a asp-action="Dashboard" class="btn btn-sm btn-outline-light bg-gradient" style="margin-right:5px;">
                        <i class="fa-solid fa-grip fa-lg fa-fw fa-beat" style="--fa-animation-duration: 2s;"></i>
                        Dashboard
                    </a>
                </span>
            </h5>
        </div>
        <input type="hidden" id="YearCode" value="" />
        <div class="card-body">
            <div class="card overflow-auto">
                <div class="card-body">
                    <form id="SParam">
                        <partial name="_SearchParamList" />
                    </form>
                </div>
            </div>
        </div>
        <div class="card-body pb-0 pt-0">
            <div class="progress" style="height: 4px;">
                <div class="progress-bar progress-bar-striped bg-info" role="progressbar" style="width: 100%" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
            </div>
        </div>
        <div class="card-body">
            <div class="card overflow-auto">
                <div class="card-body" id="divDashboardGrid">
                    <partial name="_HRAListDataGrid" />
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    <script>
            const formFields = [
            'FromDate', 'ToDate', 
            // 'DashboardType', 'MRNType',
            'DashDepartment',  'DashDesignation', 'DashEmployee', 'DashAttendanceDate', 'DashCategory',
            'search-box'
        ];

          function saveFormStateToSession() {
            formFields.forEach(field => {
                const element = document.getElementById(field);
                if (element) {
                    const value = element.value;
                    sessionStorage.setItem(`${field}Back`, value);
                }
            });
        }
                   // FromDate

        function clearFormStateFromSession() {
            formFields.forEach(field => {
                sessionStorage.removeItem(`${field}Back`);
            });
        }

        function formatDate(date) {
            var day = ("0" + date.getDate()).slice(-2);
            var month = ("0" + (date.getMonth() + 1)).slice(-2);
            var year = date.getFullYear();
            return day + "/" + month + "/" + year;
        }

        function parseDateDDMMYYYY(dateStr) {
            var parts = dateStr.split("/");
            return new Date(parts[2], parts[1] - 1, parts[0]);
        }

        function setDefaultData() {
            // var currentDate = new Date();
            // var firstDayOfMonth = new Date(currentDate.getFullYear(), currentDate.getMonth(), 1);
            // function formatDate(date) {
            //     var day = ("0" + date.getDate()).slice(-2);
            //     var month = ("0" + (date.getMonth() + 1)).slice(-2);
            //     var year = date.getFullYear();
            //     var dateString = day + "/" + month + "/" + year;
            //     return dateString;
            // }
            // //$('#FromDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", 'now');
            // var FromDate = ($('#FromDate').val() != null && $('#FromDate').val() != undefined) ? $('#FromDate').val().split("-").join("/") : formatDateToDDMMYY(firstDayOfMonth);
            // FromDate = parseDateToFormat(FromDate.split(" ")[0]);
            // $('#FromDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", FromDate);

            // var ToDate = ($('#ToDate').val() != null && $('#ToDate').val() != undefined) ? $('#ToDate').val().split("-").join("/") : formatDateToDDMMYY(currentDate);
            // ToDate = parseDateToFormat(ToDate.split(" ")[0]);
            // $('#ToDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", ToDate);
            var currentDate = new Date();
            var firstDayOfMonth = new Date(currentDate.getFullYear(), currentDate.getMonth(), 1);

            // FROM DATE
            var fromDateVal = $('#FromDate').val();
            var FromDateStr = (fromDateVal !== null && fromDateVal.trim() !== "")
                ? fromDateVal.replace(/-/g, "/")
                : formatDate(firstDayOfMonth);

            var FromDateFormatted = parseDateDDMMYYYY(FromDateStr);
            $('#FromDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", FromDateFormatted);

            // TO DATE
            var toDateVal = $('#ToDate').val();
            var ToDateStr = (toDateVal !== null && toDateVal.trim() !== "")
                ? toDateVal.replace(/-/g, "/")
                : formatDate(currentDate);

            var ToDateFormatted = parseDateDDMMYYYY(ToDateStr);
            $('#ToDate').datepicker({ dateFormat: 'dd/mm/yy' }).datepicker("setDate", ToDateFormatted);

                // AutoComplete('VDirectPurchaseBillMain', 'ItemName', 'ItemName', null);
            // AutoComplete('VDirectPurchaseBillMain', 'PurchVouchNo', 'purvoucherno', null);

        //     $('#DashboardType').select2();
            const selectIds = [
              'DashDepartment',  'DashDesignation', 'DashEmployee', 'DashAttendanceDate', 'DashCategory'
            ];

            selectIds.forEach(function (id) {
                //$('#' + id).select2();
            });

            // Restore previous selections from sessionStorage
            selectIds.forEach(function (id) {
            const storedText = sessionStorage.getItem(id + 'Back');
            if (storedText) {
                let matched = false;

                $('#' + id + ' option').each(function () {
                    if ($(this).text().trim() === storedText.trim()) {
                        $('#' + id).val($(this).val()).trigger('change');
                        matched = true;
                        return false;
                    }
                });

                if (!matched) {
                    const newOption = new Option(storedText, storedText, true, true);
                    $('#' + id).append(newOption).trigger('change');
                }
            }

            // Save on change
            $('#' + id).on('change', function () {
                const selectedText = $('#' + id + ' option:selected').text();
                sessionStorage.setItem(id + 'Back', selectedText);
            });
        });
        }

        $(function () {
            // Fields to handle
            // Save all form field values into sessionStorage
            GetFormRights();
            //LockedFinancialYear();
            CheckApprovedOrNot();
            var dashGrid = $('#HRADashboardGrid tr').length - 1;
            $('#totalRows').text(dashGrid);
            setDefaultData();
            $('#YearCode').val($('#sessionYC').text());
            /* Date change triggers */
            $(document).on('change', '#FromDate, #ToDate', reloadFilters);
            var FromDateBack = sessionStorage.getItem('FromDateBack');
            if (FromDateBack) {
                $('#FromDate').val(FromDateBack);
                if ($('#FromDate').hasClass('hasDatepicker')) {
                    $('#FromDate').datepicker('setDate', FromDateBack);
                }
            }

            // ToDate
            var ToDateBack = sessionStorage.getItem('ToDateBack');
            if (ToDateBack) {
                $('#ToDate').val(ToDateBack);
                if ($('#ToDate').hasClass('hasDatepicker')) {
                    $('#ToDate').datepicker('setDate', ToDateBack);
                }
            }

            // // DashboardType
            // var DashboardTypeBack = sessionStorage.getItem('DashboardTypeBack');
            // if (DashboardTypeBack) {
            //     $('#DashboardType').val(DashboardTypeBack).trigger('change'); // select2
            // }

            // // MRNType
            // var MRNTypeBack = sessionStorage.getItem('MRNTypeBack');
            // if (MRNTypeBack) {
            //     $('#MRNType').val(MRNTypeBack).trigger('change'); // select2
            // }
            GetSearchData();
            clearFormStateFromSession();
            reloadFilters();
        });

        function CheckApprovedOrNot() {
            var columnLength = $('#divDashboardGrid tr td').length;
            var rowLength = $('#divDashboardGrid tr').length;
            if (columnLength > 1) {
                for (var i = 1; i < rowLength; i++) {
                    var mainApproval = $('#approvedOrNot-' + i).val();
                    var firstLevelApproval = $('#fLevelApproved-' + i).val();
                    var ammApproval = $('#AmmApproved-' + i).val();
                    if ((ammApproval == "N" || ammApproval == null || ammApproval == "null" || ammApproval == "") && mainApproval == "Y") {
                        $('#approvedRow-' + i).css("color", "blue");
                    }
                    else if (mainApproval == "Y") {
                        $('#approvedRow-' + i).css("color", "green");
                    } else {
                        if (ammApproval == "Y")
                            $('#approvedRow-' + i).css("color", "blue");
                        else if (firstLevelApproval == "Y")
                            $('#approvedRow-' + i).css("color", "purple");
                        else
                            $('#approvedRow-' + i).css("color", "red");
                    }
                }
            }
        }

        function GetFormRights() {
            $.ajax({
                url: "@Url.Action("GetFormRights")",
                type: "POST",
                success: function (data) {
                    if (data != null) {
                        var obj = $.parseJSON(data);
                        if (obj.Result.Table.length == 0) {
                            var rowCounter = $('#divDashboardGrid tr').length;
                            for (var i = 0; i < rowCounter; i++) {
                                $('#viewCursorDiv-' + i).empty();
                                $('#viewCursorDiv-' + i).append(`<a class="table-link link-success noCursor" id="viewNoCursor"">
                                                                                                                                <span class="fa-stack">
                                                                                                                                    <i class="fa fa-square fa-stack-2x"></i>
                                                                                                                                    <i class="fa-solid fa-magnifying-glass fa-stack-1x fa-inverse"></i>
                                                                                                                                </span>
                                                                                                                            </a>`);
                            }
                            for (var i = 0; i < rowCounter; i++) {
                                $('#editCursorDiv-' + i).empty();
                                $('#editCursorDiv-' + i).append(`<a class="table-link link-primary noCursor">
                                                                                                                                <span class="fa-stack">
                                                                                                                                    <i class="fa fa-square fa-stack-2x"></i>
                                                                                                                                    <i class="fa fa-pencil fa-stack-1x fa-inverse"></i>
                                                                                                                                </span>
                                                                                                                            </a>`);
                            }
                            for (var i = 0; i < rowCounter; i++) {
                                $('#deleteCursorDiv-' + i).empty();
                                $('#deleteCursorDiv-' + i).append(`<a class="table-link link-danger noCursor">
                                                                                                                                <span class="fa-stack">
                                                                                                                                    <i class="fa fa-square fa-stack-2x"></i>
                                                                                                                                    <i class="fa-solid fa-trash fa-stack-1x fa-inverse"></i>
                                                                                                                                </span>
                                                                                                                            </a>`);
                            }
                        } else {
                            console.log(obj);
                            var optAll = obj.Result.Table[0].OptAll;
                            var optSave = obj.Result.Table[0].OptSave;
                            var optUpdate = obj.Result.Table[0].OptUpdate;
                            var optDelete = obj.Result.Table[0].OptDelete;
                            var optView = obj.Result.Table[0].OptView;
                            var rowCounter = $('#divDashboardGrid tr').length;
                            if (!optAll) {
                                if (!optView) {

                                    for (var i = 0; i < rowCounter; i++) {
                                        $('#viewCursorDiv-' + i).empty();
                                        $('#viewCursorDiv-' + i).append(`<a class="table-link link-success noCursor" id="viewNoCursor"">
                                                                                                                        <span class="fa-stack">
                                                                                                                            <i class="fa fa-square fa-stack-2x"></i>
                                                                                                                            <i class="fa-solid fa-magnifying-glass fa-stack-1x fa-inverse"></i>
                                                                                                                        </span>
                                                                                                                    </a>`);
                                    }
                                }
                                if (!optUpdate) {
                                    for (var i = 0; i < rowCounter; i++) {
                                        $('#editCursorDiv-' + i).empty();
                                        $('#editCursorDiv-' + i).append(`<a class="table-link link-primary noCursor">
                                                           <span class="fa-stack">
                                                               <i class="fa fa-square fa-stack-2x"></i>
                                                               <i class="fa fa-pencil fa-stack-1x fa-inverse"></i>
                                                           </span>
                                                        </a>`);
                                    }
                                }
                                // if (!optDelete) {
                                //     for (var i = 0; i < rowCounter; i++) {
                                //         $('#deleteCursorDiv-' + i).empty();
                                //         $('#deleteCursorDiv-' + i).append(`<a class="table-link link-danger noCursor">
                                //                                                                         <span class="fa-stack">
                                //                                                                             <i class="fa fa-square fa-stack-2x"></i>
                                //                                                                             <i class="fa-solid fa-trash fa-stack-1x fa-inverse"></i>
                                //                                                                         </span>
                                //                                                                     </a>`);
                                //     }
                                // }
                            }
                        }
                    }
                },
                error: function (errMsg) {
                    $.notify(errMsg, { position: "top center", className: "error" });
                }
            });
        }

        $('#sendToExcel').click(function () {
            $("#HRADashboardGrid").table2excel({
                filename: "HRAttendanceTable.xls"
            });
        });

        var currentSearchColumn = null;

        $(document).on("input", "#search-box", function () {
            var value = $(this).val().toLowerCase();

            $("#divDashboardGrid1 tr").filter(function () {
                if (currentSearchColumn !== null) {
                    var columnText = $(this).find('td').eq(currentSearchColumn).text().toLowerCase();
                    $(this).toggle(columnText.indexOf(value) > -1);
                } else {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
                }
            });
        });

        $(document).on("click", "th[data-columnno]", function () {
            currentSearchColumn = $(this).data('columnno');
            $("#search-box").val('').trigger('keyup'); // Reset search box and trigger the search
        });

        $(document).on("click", ".sortCol", function () {
            var $column = $(this).parent().closest("th");
            var colno = $(this).data('columnno');
            var currentOrder = $(this).data('sortorder');
            $('.sortCol').css('color', 'white');
            if (currentOrder === 'asc') {
                sortTable(colno, false);
                $(this).data('sortorder', 'desc');
                $(this).css('color', 'yellow'); // Visual indication of descending order
                //$(this).css('font-size', 'larger'); // Visual indication of descending order
                $(this).find('i:first').removeClass('fa-arrow-up');
                $(this).find('i:first').addClass('fa-arrow-down');
            } else {
                sortTable(colno, true);
                $(this).data('sortorder', 'asc');
                $(this).css('color', 'yellow');
                // $(this).css('font-size', 'larger'); // Visual indication of ascending order
                $(this).find('i:first').removeClass('fa-arrow-down');
                $(this).find('i:first').addClass('fa-arrow-up');
            }
        });
        function sortTable(columnIndex, ascending) {

            var table = $('#HRADashboardGrid');
            var rows = table.find('#divDashboardGrid1 > tr').toArray();

            rows.sort(function (a, b) {
                var aValue = $(a).find('td').eq(columnIndex).text();
                var bValue = $(b).find('td').eq(columnIndex).text();

                var aIntValue = 0;
                var bIntValue = 0;
                if (/^\d*\.?\d+$/.test(aValue) && /^\d*\.?\d+$/.test(bValue)) {
                    aIntValue = parseInt(aValue, 10);
                    bIntValue = parseInt(bValue, 10);
                }

                if (aIntValue > 0) {
                    if (ascending) {
                        return aValue - bValue;
                    } else {
                        return bValue - aValue;
                    }
                } else {
                    if (ascending) {
                        return aValue.localeCompare(bValue);
                    } else {
                        return bValue.localeCompare(aValue);
                    }
                }
            });

            $.each(rows, function (index, row) {
                table.children('#divDashboardGrid1').append(row);
            });
        }

        function LockedFinancialYear() {
            $.ajax({
                type: "GET",
                url: "@Url.Action("CheckLockYear")",
                dataType: "json",
                data: { "YearCode": $('#sessionYC').text() },
                success: function (result, status, error) {
                    console.log(result);
                    var obj = $.parseJSON(result);
                    if (obj.Result != null && obj.Result.Table[0] != undefined) {
                        if (obj.Result.Table[0].ClosedForAllusers == 'Y') {
                            var rowCount = $('#divDashboardGrid tr').length;
                            for (var i = 0; i < rowCount; i++) {
                                $('#editCursorDiv-' + i).empty();
                                $('#editCursorDiv-' + i).append(`<a class="table-link link-primary noCursor">
                                                    <span class="fa-stack">
                                                        <i class="fa fa-square fa-stack-2x"></i>
                                                        <i class="fa fa-pencil fa-stack-1x fa-inverse"></i>
                                                    </span>
                                                 </a>`);

                                // $('#deleteCursorDiv-' + i).empty();

                                // $('#deleteCursorDiv-' + i).append(`<a class="table-link link-danger noCursor">
                                //     <span class="fa-stack">
                                //         <i class="fa fa-square fa-stack-2x"></i>
                                //         <i class="fa-solid fa-trash fa-stack-1x fa-inverse"></i>
                                //     </span>
                                // </a>`);
                            }
                        }
                    }
                }
            });
        }

        /* -----------------------------------------
           AJAX FILTER LOADERS
        ----------------------------------------- */
         function reloadFilters() {
            $.ajax({
                url: '@Url.Action("RefreshFilters")',
                type: 'POST',
                data: {
                    YearCode: $('#HRAttYearCode').val(),
                    fromDate: $('#FromDate').val(),
                    toDate: $('#ToDate').val(),
                    employeeId: $('#DashEmployee').val()
                },
                success: function (res) {
                    // Attendance Dates
                    let dateSel = $("#DashAttendanceDate");
                    dateSel.empty().append(`<option value="0">-Select-</option>`);
                    res.attendanceDates.forEach(x => {
                            let onlyDate = x.text.split(" ")[0];   // ⬅ removes time
                            dateSel.append(`<option value="${x.value}">${onlyDate}</option>`);
                    });

                    // Category
                    let catSel = $("#DashCategory");
                    catSel.empty().append(`<option value="0">-Select-</option>`);
                    res.categories.forEach(x => {
                        catSel.append(`<option value="${x.value}">${x.text}</option>`);
                    });
                }
            });
        }


        /* -----------------------------------------
           AJAX LIST LOADERS
        ----------------------------------------- */
        function GetSearchData() { GetListData(); }

        function GetListData() {
            $.ajax({
                url: '@Url.Action("GetSearchHRAListData")',
                type: "POST",
                data: $('#SParam').serializeArray(),
                success: function (data) {
                    //LockedFinancialYear();
                    $('#divDashboardGrid').empty().append(data);
                    CheckApprovedOrNot();
                    var rows = $('#HRADashboardGrid tr').length - 1;
                    $('#totalRows').text(rows);
                    // reloadFilters();
                },
                error: function (response) {
                    alert(response.responseText);
                },
                failure: function (response) {
                    alert(response.responseText);
                }
            });
        }
        /* -----------------------------------------
           TRANSFER TO EDIT PAGE
        ----------------------------------------- */
        function transferDataToEdit(i) {
            saveFormStateToSession();
            showToTransfer(i);
        }

        function showToTransfer(i) {
            var yr = $('#HRAttYearCode_' + i).text();
            //alert('mrnyear '+mrnYear);
            const currentYear = $('#YearCode').val();
            if (yr == currentYear) {
                var url = '/HRAttendance/Index/?';
                url += "ID=" + encodeURIComponent(0) +
                    "&YC=" + encodeURIComponent(currentYear) +
                    "&Mode=" + encodeURIComponent('HRAI') +
                    "&flag=" + encodeURIComponent('PendingGateAttendance') +
                    "&hrattyearcode=" + encodeURIComponent($('#HRAttYearCode_' + i).text());

                // Additional parameters for URL construction
                url += "&FromDate=" + encodeURIComponent($('#FromDate').val()) +
                    "&ToDate=" + encodeURIComponent($('#ToDate').val()) +
                    "&REQNo=" + encodeURIComponent($('#ReqNo').val()) +
                    "&ReqYearCode=" + encodeURIComponent($('#ReqYearCode').val()) +
                    "&GlobalSearch=" + encodeURIComponent($('#search-box').val());

                // Redirect to the constructed URL
                window.location.href = url;
            }
            else {
                $.notify("Attendance Year is not equals to Current Logged In current Year");
            }
        }

        /*----------------------------------------------
        pagination
        --------------------------------------------- */
        function loadPage(pageNumber) {
            //var dashboardType = $('#DashboardType').val();
            var url = '@Url.Action("GetSearchHRAListData", "HRAttendance")';

            var formData = $('#SParam').serializeArray();
            formData.push({ name: 'pageNumber', value: pageNumber });
            $.ajax({
                type: 'GET',
                url: url,
                data: formData,
                success: function (result) {
                    $('#divDashboardGrid').empty();
                    $('#divDashboardGrid').html(result);
                    CheckApprovedOrNot();
                    $('#totalRows').text($('#divDashboardGrid1 tr').length);
                    //countAllTotal();
                }
            });
        }
        $("#search-box").on("input", function () {
            var searchString = $(this).val().trim();
           // var dashboardType = $('#DashboardType').val();
            $.ajax({
                url: "/HRAttendance/GlobalSearch?searchString=" + searchString,
                type: "GET",
                data: { searchString: searchString /* , dashboardType: dashboardType */ },
                success: function (data) {
                    $('#divDashboardGrid').empty();
                    $('#divDashboardGrid').html(data);
                    CheckApprovedOrNot();
                    $('#totalRows').text($('#divDashboardGrid1 tr').length);
                    //countAllTotal();
                },
                error: function () {
                    console.log("Error fetching search results.");
                }
            });
        });
        var currentSearchColumn = null;
        $("#search-box").on("keyup", function () {
            var value = $(this).val().toLowerCase();
            $("#divDashboardGrid1 tr").filter(function () {
                if (currentSearchColumn !== null) {
                    var columnText = $(this).find('td').eq(currentSearchColumn).text().toLowerCase();
                    $(this).toggle(columnText.indexOf(value) > -1);
                } else {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
                }
            });
        });

        $(document).on("click", "th[data-columnno]", function () {
            currentSearchColumn = $(this).data('columnno');
            $("#search-box").val('').trigger('keyup'); // Reset search box and trigger the search
        });

        $(document).on("click", ".sortCol", function () {
            var $column = $(this).parent().closest("th");
            var colno = $(this).data('columnno');
            var currentOrder = $(this).data('sortorder');

            $('.sortCol').css('color', 'white');
            if (currentOrder === 'asc') {
                sortTable(colno, false);
                $(this).data('sortorder', 'desc');
                $(this).css('color', 'yellow'); // Visual indication of descending order
                //$(this).css('font-size', 'larger'); // Visual indication of descending order
                $(this).find('i:first').removeClass('fa-arrow-up');
                $(this).find('i:first').addClass('fa-arrow-down');
            } else {
                sortTable(colno, true);
                $(this).data('sortorder', 'asc');
                $(this).css('color', 'yellow');
                // $(this).css('font-size', 'larger'); // Visual indication of ascending order
                $(this).find('i:first').removeClass('fa-arrow-down');
                $(this).find('i:first').addClass('fa-arrow-up');
            }
        });
        function sortTable(columnIndex, ascending) {
            var table = $('#HRADashboardGrid');
            var rows = table.find('#divDashboardGrid1 > tr').toArray();

            rows.sort(function (a, b) {
                var aValue = $(a).find('td').eq(columnIndex).text();
                var bValue = $(b).find('td').eq(columnIndex).text();
                var aIntValue = 0;
                var bIntValue = 0;
                if (/^\d*\.?\d+$/.test(aValue) && /^\d*\.?\d+$/.test(bValue)) {
                    aIntValue = parseInt(aValue, 10);
                    bIntValue = parseInt(bValue, 10);
                }
                if (aIntValue > 0) {
                    if (ascending) {
                        return aValue - bValue;
                    } else {
                        return bValue - aValue;
                    }
                } else {
                    if (ascending) {
                        return aValue.localeCompare(bValue);
                    } else {
                        return bValue.localeCompare(aValue);
                    }
                }
            });
            $.each(rows, function (index, row) {
                table.children('#divDashboardGrid1').append(row);
            });
        }
    </script>
    <script src="~/Scripts/table2excel.js"></script>
}